import type { Test } from '../../types/quiz';

export const midDesign: Test = {
  id: 'mid-design',
  role: 'mid',
  title: 'Software Design & Refactoring',
  description: 'SOLID principles, design patterns, coupling, cohesion, and safe refactoring strategies.',
  topics: ['SOLID', 'Design Patterns', 'Refactoring', 'Coupling', 'Cohesion'],
  estimatedMinutes: 25,
  questions: [
    {
      id: 'md-001',
      topic: 'SOLID',
      difficulty: 'easy',
      prompt: 'What does the Single Responsibility Principle (SRP) require?',
      options: [
        { id: 'a', text: 'Each function must be a single line' },
        { id: 'b', text: 'A class should have only one reason to change, meaning it should only have one job' },
        { id: 'c', text: 'All classes must be in a single file' },
        { id: 'd', text: 'Only one class should be responsible for the entire application' },
      ],
      correctOptionId: 'b',
      definition: 'The Single Responsibility Principle (SRP) states that a class should have only one reason to change — it should be responsible for one cohesive area of behavior, so changes to one part of the system only affect one class.',
      explanation: 'SRP is not about function length but about cohesion of purpose. A class that handles both business logic and database persistence has two reasons to change: when business rules change and when the database schema changes. Separating these concerns makes each class smaller, more focused, easier to test, and less likely to break unrelated functionality when modified.',
      whyOthersAreWrong: [
        'Option A: SRP imposes no constraint on function length; a single-responsibility function may legitimately require many lines of code.',
        'Option C: File organization is a project convention; SRP is about the semantic responsibility of a class, not where files are stored.',
        'Option D: This would be the opposite of SRP — concentrating all responsibility in one class is an anti-pattern (God class) that SRP is designed to prevent.',
      ],
      example: 'A `ReportGenerator` that both formats a report and emails it has two responsibilities. SRP says split it: `ReportFormatter` (transforms data to a report) and `ReportEmailer` (sends the report). Each class changes only when its specific concern changes.',
    },
    {
      id: 'md-002',
      topic: 'SOLID',
      difficulty: 'medium',
      prompt: 'What does the Open/Closed Principle state?',
      options: [
        { id: 'a', text: 'Classes should be open for modification and closed for extension' },
        { id: 'b', text: 'Software entities should be open for extension but closed for modification' },
        { id: 'c', text: 'Open-source code should never be modified' },
        { id: 'd', text: 'Public APIs should be closed once released' },
      ],
      correctOptionId: 'b',
      definition: 'The Open/Closed Principle (OCP) states that software entities (classes, modules, functions) should be open for extension (you can add new behavior) but closed for modification (you do not change existing tested code to do so).',
      explanation: 'OCP is achieved through abstraction — using interfaces or abstract classes so you can add new implementations without touching existing code. For example, adding a new payment method should not require modifying existing payment processing code. This prevents regressions in tested code when adding new features.',
      whyOthersAreWrong: [
        'Option A: This is backwards — OCP says open for extension, closed for modification, not the other way around.',
        'Option C: OCP is an object-oriented design principle; it says nothing about open-source software licensing or modification policies.',
        'Option D: Closing public APIs refers to API versioning strategy; OCP is about the internal design of software entities to support extension without modification.',
      ],
      example: 'A discount calculator with `if (type === "vip") ... else if (type === "seasonal") ...` violates OCP — adding a new discount type requires modifying the function. OCP fix: an `IDiscountStrategy` interface with `apply(price)`, each discount type as a separate class. Adding a new discount = adding a new class, no existing code touched.',
    },
    {
      id: 'md-003',
      topic: 'SOLID',
      difficulty: 'medium',
      prompt: 'What is the Liskov Substitution Principle (LSP)?',
      options: [
        { id: 'a', text: 'Subtypes must be fully replaceable for their base types without altering the correctness of the program' },
        { id: 'b', text: 'Every subclass must override all parent methods' },
        { id: 'c', text: 'Base classes must not depend on derived classes' },
        { id: 'd', text: 'Interfaces must be substituted for abstract classes' },
      ],
      correctOptionId: 'a',
      definition: 'The Liskov Substitution Principle states that objects of a derived class must be substitutable for objects of the base class without breaking the correctness of the program — derived types must honor the contract of the base type.',
      explanation: 'LSP violations occur when a subclass weakens preconditions, strengthens postconditions, or throws exceptions not thrown by the base class. The classic example is a `Square` extending `Rectangle` where setting width also sets height — code that expects `Rectangle` behavior breaks when given a `Square`. LSP ensures inheritance represents genuine "is-a" relationships.',
      whyOthersAreWrong: [
        'Option B: Overriding all parent methods is not required by LSP and would often violate it by changing behavior that callers depend on.',
        'Option C: Base classes not depending on derived classes is the Dependency Inversion Principle (DIP), not LSP.',
        'Option D: Substituting interfaces for abstract classes is an implementation choice; LSP is about behavioral contracts in inheritance hierarchies.',
      ],
      example: 'LSP violation: `Bird.fly()` works for `Eagle` but `Penguin extends Bird` cannot fly. Code that calls `bird.fly()` for any `Bird` breaks with a `Penguin`. LSP fix: do not put `fly()` in `Bird`; use `FlyingBird extends Bird` and `NonFlyingBird extends Bird` separately.',
    },
    {
      id: 'md-004',
      topic: 'SOLID',
      difficulty: 'medium',
      prompt: 'What does the Interface Segregation Principle say?',
      options: [
        { id: 'a', text: 'Interfaces should have as many methods as possible' },
        { id: 'b', text: 'Clients should not be forced to depend on interfaces they do not use — prefer small, focused interfaces' },
        { id: 'c', text: 'Each class should implement exactly one interface' },
        { id: 'd', text: 'Interfaces should be segregated by access level' },
      ],
      correctOptionId: 'b',
      definition: 'The Interface Segregation Principle (ISP) states that clients should not be forced to implement or depend on methods they do not use — large "fat" interfaces should be split into smaller, more specific ones.',
      explanation: 'When a class is forced to implement interface methods it does not need, it creates coupling and confusion — the class might throw NotImplementedException for unused methods. ISP promotes role interfaces: small interfaces that represent a specific capability. Clients depend only on the capabilities they actually need.',
      whyOthersAreWrong: [
        'Option A: Interfaces with as many methods as possible is exactly the fat interface anti-pattern ISP is designed to prevent.',
        'Option C: The number of interfaces a class implements is not constrained; a class may implement many small, focused interfaces (which is encouraged by ISP).',
        'Option D: Access levels (public, private) are a language visibility feature; ISP is about the cohesion and granularity of interface methods.',
      ],
      example: 'A fat `IWorker` interface with `work()`, `eat()`, and `sleep()` forces a `Robot` class to implement `eat()` and `sleep()` as stubs. ISP fix: split into `IWorkable`, `IFeedable`, `ISleepable`. `Robot` implements only `IWorkable`; `HumanWorker` implements all three.',
    },
    {
      id: 'md-005',
      topic: 'SOLID',
      difficulty: 'medium',
      prompt: 'What is the Dependency Inversion Principle?',
      options: [
        { id: 'a', text: 'High-level modules should depend on low-level modules directly' },
        { id: 'b', text: 'High-level modules should depend on abstractions, not on concrete implementations' },
        { id: 'c', text: 'Dependencies should be inverted at runtime' },
        { id: 'd', text: 'All dependencies must be injected via the constructor' },
      ],
      correctOptionId: 'b',
      definition: 'The Dependency Inversion Principle (DIP) states that high-level modules should not depend on low-level modules; both should depend on abstractions. Abstractions should not depend on details; details should depend on abstractions.',
      explanation: 'Without DIP, a business logic class that directly instantiates a database class creates a tight coupling — you cannot test the business logic without a real database, and swapping databases requires changing business logic code. By depending on an interface (`IRepository`), the business logic is decoupled from the specific implementation, enabling substitution and testability.',
      whyOthersAreWrong: [
        'Option A: High-level depending on low-level directly is the problem DIP solves — this creates tight coupling and prevents substitution.',
        'Option C: "Inverting at runtime" is not a meaningful phrase in this context; DIP is about the direction of source code dependencies, resolved at design time through abstractions.',
        'Option D: Constructor injection is a common dependency injection pattern that supports DIP, but DIP itself does not require constructor injection specifically — property injection and method injection are also valid.',
      ],
      example: 'Without DIP: `class OrderService { private db = new MySQLDatabase(); }` — tightly coupled. With DIP: `class OrderService { constructor(private db: IDatabase) {} }`. Tests inject `MockDatabase`, production injects `MySQLDatabase` — business logic unchanged.',
    },
    {
      id: 'md-006',
      topic: 'Design Patterns',
      difficulty: 'medium',
      prompt: 'What problem does the Factory pattern solve?',
      options: [
        { id: 'a', text: 'Managing object state changes' },
        { id: 'b', text: 'Decoupling object creation from the code that uses the objects, allowing flexible instantiation' },
        { id: 'c', text: 'Ensuring only one instance of a class exists' },
        { id: 'd', text: 'Providing a simplified interface to a complex subsystem' },
      ],
      correctOptionId: 'b',
      definition: 'The Factory pattern provides an interface for creating objects without specifying their exact concrete class, decoupling the creation logic from the consumer code.',
      explanation: 'Without a factory, calling `new ConcreteClass()` throughout the codebase couples every caller to that specific implementation. A factory centralizes creation logic, allows conditional instantiation (e.g., returning different implementations based on config), and hides construction complexity. When the concrete type changes, only the factory changes.',
      whyOthersAreWrong: [
        'Option A: Managing object state changes is the State pattern; the Factory pattern is about instantiation, not state transitions.',
        'Option C: Ensuring a single instance is the Singleton pattern; Factory creates potentially many objects with flexible types.',
        'Option D: Simplifying access to a complex subsystem is the Facade pattern; Factory is specifically about object creation.',
      ],
      example: 'A `LoggerFactory.create(config)` returns a `FileLogger`, `ConsoleLogger`, or `CloudLogger` depending on the config. Callers use `ILogger` and never know which concrete logger they have. Switching all logging to a cloud provider requires changing only the factory.',
    },
    {
      id: 'md-007',
      topic: 'Design Patterns',
      difficulty: 'medium',
      prompt: 'What is the Repository pattern?',
      options: [
        { id: 'a', text: 'A pattern for managing UI state' },
        { id: 'b', text: 'An abstraction layer between the domain logic and data access, making it easy to swap storage implementations' },
        { id: 'c', text: 'A pattern for creating multiple objects of the same type' },
        { id: 'd', text: 'A way to cache database results' },
      ],
      correctOptionId: 'b',
      definition: 'The Repository pattern provides an abstraction layer that mediates between the domain model and data mapping layers, presenting a collection-like interface for accessing domain objects while hiding the underlying data access details.',
      explanation: 'Repositories encapsulate queries, ORM calls, and SQL behind a clean interface like `IUserRepository` with methods like `findById(id)`, `save(user)`, and `delete(id)`. Domain/service logic depends only on the interface, not on PostgreSQL, MongoDB, or any specific ORM. This enables unit testing with in-memory repositories and makes storage migrations transparent to business logic.',
      whyOthersAreWrong: [
        'Option A: UI state management is handled by state management libraries (Redux, MobX, Zustand); the Repository pattern is for data persistence abstraction.',
        'Option C: Creating multiple objects of the same type is the Factory or Builder pattern; Repository is about data access abstraction, not object creation.',
        'Option D: Caching is a concern that can be layered on top of a repository, but caching itself is not the Repository pattern\'s purpose.',
      ],
      example: 'Service layer: `class UserService { constructor(private repo: IUserRepository) {} async getUser(id: string) { return this.repo.findById(id); } }`. Tests inject `InMemoryUserRepository`; production uses `PostgresUserRepository`. The service code never changes when storage changes.',
    },
    {
      id: 'md-008',
      topic: 'Design Patterns',
      difficulty: 'medium',
      prompt: 'What is the Observer pattern?',
      options: [
        { id: 'a', text: 'An object that monitors system performance' },
        { id: 'b', text: 'A pattern where an object (subject) maintains a list of dependents (observers) and notifies them of state changes' },
        { id: 'c', text: 'A pattern for caching expensive computations' },
        { id: 'd', text: 'A pattern for converting one interface to another' },
      ],
      correctOptionId: 'b',
      definition: 'The Observer pattern defines a one-to-many dependency between objects so that when one object (the subject) changes state, all its dependents (observers) are notified and updated automatically.',
      explanation: 'Observer enables loose coupling: the subject does not know the concrete types of its observers, only that they implement the observer interface. This is the foundation of event systems, reactive programming, and pub/sub messaging. UI frameworks (DOM events, React state), Node.js EventEmitter, and RxJS are all implementations of Observer.',
      whyOthersAreWrong: [
        'Option A: Monitoring system performance is the role of APM tools and metrics collection; the Observer pattern is a software design pattern about dependency notification.',
        'Option C: Caching expensive computations is the Flyweight or memoization pattern; Observer is about notifying dependents of state changes.',
        'Option D: Converting one interface to another is the Adapter pattern; Observer is about broadcasting state changes to subscribers.',
      ],
      example: 'In TypeScript: `class EventEmitter<T> { private listeners: Array<(data: T) => void> = []; subscribe(fn: (data: T) => void) { this.listeners.push(fn); } emit(data: T) { this.listeners.forEach(fn => fn(data)); } }`. UI buttons, WebSocket message handlers, and state stores all use this pattern.',
    },
    {
      id: 'md-009',
      topic: 'SOLID',
      difficulty: 'medium',
      prompt: 'When should you prefer composition over inheritance?',
      options: [
        { id: 'a', text: 'When you need to reuse a class without changing its behavior' },
        { id: 'b', text: 'When you want to model a "has-a" relationship and combine behaviors flexibly without deep class hierarchies' },
        { id: 'c', text: 'When two classes share identical method signatures' },
        { id: 'd', text: 'When performance is critical' },
      ],
      correctOptionId: 'b',
      definition: 'Composition over inheritance is a design principle that favors building complex behavior by combining simple objects (composition) rather than extending class hierarchies, because it provides more flexibility and avoids tight coupling between parent and child classes.',
      explanation: 'Inheritance creates rigid is-a hierarchies that are hard to change; changing the base class can break all subclasses. Composition (has-a) lets you mix and match behaviors by holding references to collaborators. It enables runtime behavior changes by swapping composited objects and avoids the fragile base class problem.',
      whyOthersAreWrong: [
        'Option A: Reusing a class without changing behavior can be done with either composition or delegation; this does not specifically argue for composition over inheritance.',
        'Option C: Shared method signatures define an interface or protocol, not a reason to choose composition over inheritance specifically.',
        'Option D: Performance is rarely determined by composition vs inheritance; both involve method dispatch overhead. Performance optimization is a separate concern.',
      ],
      example: 'Instead of `class FlyingFishingBoat extends FlyingVehicle extends WaterVehicle` (multiple inheritance problem), use composition: `class Vehicle { constructor(private engine: IEngine, private mover: IMover) {} }`. Combine a `PropellerMover` and `WingMover` at runtime for a seaplane without deep hierarchies.',
    },
    {
      id: 'md-010',
      topic: 'Cohesion',
      difficulty: 'medium',
      prompt: 'What does "high cohesion" mean in software design?',
      options: [
        { id: 'a', text: 'A module has many dependencies' },
        { id: 'b', text: 'The elements inside a module are closely related and focused on a single well-defined purpose' },
        { id: 'c', text: 'A module can communicate with many other modules' },
        { id: 'd', text: 'A class has a large public interface' },
      ],
      correctOptionId: 'b',
      definition: 'Cohesion measures how strongly the elements within a module (class, function, package) are related to one another. High cohesion means all elements contribute to a single, well-defined responsibility.',
      explanation: 'High cohesion is desirable because it makes modules easier to understand (they do one thing), easier to reuse (they are self-contained), and easier to maintain (changes to one concern do not ripple into unrelated functionality). Low cohesion — mixing unrelated responsibilities — is often the root cause of code smells like God classes.',
      whyOthersAreWrong: [
        'Option A: Having many dependencies (imports from many modules) describes high coupling, not cohesion; cohesion is about internal relatedness within a module.',
        'Option C: Communicating with many other modules also describes high coupling (how modules interact with each other); cohesion is about what is inside a single module.',
        'Option D: A large public interface suggests low cohesion and potentially too many responsibilities; high cohesion typically correlates with a small, focused interface.',
      ],
      example: 'A `DateFormatter` class with methods `toISO()`, `toLocale()`, and `toRelative()` has high cohesion — all methods relate to date formatting. Adding `sendEmail()` to `DateFormatter` would destroy cohesion because email sending is unrelated to date formatting.',
    },
    {
      id: 'md-011',
      topic: 'Coupling',
      difficulty: 'medium',
      prompt: 'Why is low coupling between modules desirable?',
      options: [
        { id: 'a', text: 'It makes the codebase smaller' },
        { id: 'b', text: 'It allows modules to be changed or replaced independently without cascading changes through the system' },
        { id: 'c', text: 'It makes code run faster' },
        { id: 'd', text: 'It reduces the number of classes needed' },
      ],
      correctOptionId: 'b',
      definition: 'Coupling measures the degree of interdependence between modules. Low coupling means modules depend on as little as possible about each other, making them easier to change, test, and replace independently.',
      explanation: 'High coupling creates ripple effects — changing one module requires changing all modules that depend on its internal details. Low coupling, achieved through abstractions and narrow interfaces, means you can change the implementation of a module without affecting its consumers. This is the key enabler of maintainability, testability, and team autonomy.',
      whyOthersAreWrong: [
        'Option A: Low coupling does not inherently reduce code size — decoupled systems may require more interfaces and adapter classes.',
        'Option C: Coupling has negligible effect on runtime performance; it is a code organization and maintainability concern.',
        'Option D: Low coupling often requires more classes (interfaces, adapters, factories) rather than fewer; the benefit is independence, not class count.',
      ],
      example: 'Tightly coupled: `OrderService` directly imports and uses `MySQLOrderRepository`. Loosely coupled: `OrderService` depends on `IOrderRepository` interface; `MySQLOrderRepository` and `InMemoryOrderRepository` both implement it. Switching the database requires zero changes to `OrderService`.',
    },
    {
      id: 'md-012',
      topic: 'Refactoring',
      difficulty: 'easy',
      prompt: 'What is a code smell?',
      options: [
        { id: 'a', text: 'A syntax error' },
        { id: 'b', text: 'A surface indication that suggests there may be a deeper design problem in the code' },
        { id: 'c', text: 'A runtime exception' },
        { id: 'd', text: 'An unused variable' },
      ],
      correctOptionId: 'b',
      definition: 'A code smell is a surface-level characteristic of code that may indicate a deeper structural or design problem, without necessarily being a bug — it is a hint that refactoring may be warranted.',
      explanation: 'Code smells like duplicated code, long methods, large classes, and excessive parameters are not bugs but signal that the code will be hard to maintain and extend. They are the impetus for refactoring. Martin Fowler\'s "Refactoring" catalogues dozens of specific code smells and the corresponding refactoring techniques to address them.',
      whyOthersAreWrong: [
        'Option A: A syntax error prevents compilation; it is a definite, immediate bug, not a smell that hints at a deeper design issue.',
        'Option C: A runtime exception is a concrete failure; a code smell is a structural indicator, not an execution failure.',
        'Option D: An unused variable is a specific linter warning; while it could be a smell, code smells encompass much broader design concerns like poor structure, excessive complexity, and misaligned responsibilities.',
      ],
      example: '"Feature Envy" is a code smell where a method seems more interested in another class\'s data than its own. `OrderPrinter.printOrder(order)` calling `order.customer.address.city` extensively smells of Feature Envy — that logic probably belongs in `Order` or `Customer`.',
    },
    {
      id: 'md-013',
      topic: 'Refactoring',
      difficulty: 'medium',
      prompt: 'When is the right time to refactor?',
      options: [
        { id: 'a', text: 'Only before a major release' },
        { id: 'b', text: 'Continuously, when you understand the code better, when tests fail, or when adding a feature would be much harder without it' },
        { id: 'c', text: 'Only when performance is unacceptable' },
        { id: 'd', text: 'Never — refactoring introduces bugs' },
      ],
      correctOptionId: 'b',
      definition: 'Refactoring is most effective when practiced continuously as part of normal development — the "boy scout rule" says to leave code cleaner than you found it each time you touch it.',
      explanation: 'The Rule of Three (from Martin Fowler) suggests: the first time you write something, just do it; the second time you do something similar, cringe but do it again; the third time you do something similar, refactor. Refactoring before adding a new feature (to make the feature easier to add) or after (to clean up the implementation) is the sustainable cadence.',
      whyOthersAreWrong: [
        'Option A: Waiting until before a major release means the debt accumulates and the pre-release refactoring is risky and stressful. Continuous small refactors are safer.',
        'Option C: Performance optimization is one narrow reason to refactor; most refactoring is for readability, maintainability, and design quality, not performance.',
        'Option D: With a safety net of automated tests, refactoring is low-risk. Avoiding refactoring causes technical debt to accumulate, making future changes far more likely to introduce bugs.',
      ],
      example: 'You are about to add a feature to a function. The function is 200 lines long and hard to follow. Refactor first — extract sub-functions, clarify names — then add the feature cleanly. The tests stay green throughout because you only change structure, not behavior.',
    },
    {
      id: 'md-014',
      topic: 'Refactoring',
      difficulty: 'hard',
      prompt: 'What is the Strangler Fig pattern?',
      options: [
        { id: 'a', text: 'A design pattern for managing database migrations' },
        { id: 'b', text: 'Incrementally replacing a legacy system by routing new requests to new code while the old code is gradually retired' },
        { id: 'c', text: 'A method for removing dead code from a large codebase' },
        { id: 'd', text: 'A pattern for monitoring production systems' },
      ],
      correctOptionId: 'b',
      definition: 'The Strangler Fig pattern (coined by Martin Fowler) is a migration technique where a new system gradually replaces a legacy system by intercepting calls to the old system, routing new or migrated functionality to the new system, until the legacy system can be decommissioned.',
      explanation: 'Named after the strangler fig tree that grows around a host tree, the pattern allows continuous delivery throughout a migration. You place a facade (a proxy/gateway) in front of the legacy system, route specific functionality to the new system incrementally, and shrink the legacy surface until nothing routes to it. This avoids the risk of a "big bang" rewrite where the new system replaces everything at once.',
      whyOthersAreWrong: [
        'Option A: Database migrations are managed with migration scripts and tools like Flyway or Liquibase; the Strangler Fig is about replacing application logic, not schema.',
        'Option C: Removing dead code is a refactoring step; the Strangler Fig is a macro-architectural migration pattern for live production systems.',
        'Option D: Production monitoring is handled by APM tools and alerting systems; the Strangler Fig is a software migration strategy.',
      ],
      example: 'Migrating a monolith to microservices: add an API gateway in front of the monolith. When you build a new `PaymentService`, configure the gateway to route all `/payments/*` requests to it instead of the monolith. The monolith\'s payment code is now unreachable and can be deleted. Repeat for each domain.',
    },
    {
      id: 'md-015',
      topic: 'Refactoring',
      difficulty: 'medium',
      prompt: 'How can feature flags help during refactoring?',
      options: [
        { id: 'a', text: 'They speed up compilation' },
        { id: 'b', text: 'They allow new code paths to be deployed but disabled in production until ready, enabling safe incremental rollouts' },
        { id: 'c', text: 'They automatically roll back failing deployments' },
        { id: 'd', text: 'They prevent merge conflicts during refactoring' },
      ],
      correctOptionId: 'b',
      definition: 'Feature flags (feature toggles) are runtime switches that control which code paths are active, allowing teams to deploy code that is disabled in production until it is fully ready — decoupling deployment from release.',
      explanation: 'During refactoring, you can deploy the new implementation behind a flag, run it for 1% of users to validate, then ramp up to 100%. If issues arise, flipping the flag off is instant — no redeployment needed. This is safer than deploying the refactored code directly to all users at once.',
      whyOthersAreWrong: [
        'Option A: Feature flags are runtime configuration; they have no effect on compilation speed.',
        'Option C: Feature flags enable instant rollback by toggling the flag, but the rollback is manual — they do not automatically detect and revert failing deployments (that would be a CD pipeline feature or circuit breaker).',
        'Option D: Merge conflicts are a source control issue resolved by rebasing or merging; feature flags do not affect Git operations.',
      ],
      example: 'Refactoring the checkout flow: deploy the new flow behind `FLAG_NEW_CHECKOUT = false`. Enable for 5% of users in staging. Monitor error rates. Enable for 100% when confident. If something breaks in production, set `FLAG_NEW_CHECKOUT = false` — instant rollback without a code deployment.',
    },
  ],
};
