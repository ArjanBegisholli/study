import type { Test } from '../../types/quiz';

export const foundationWebDatabase: Test = {
  id: 'foundation-web-database',
  role: 'foundation',
  title: 'Web, APIs & Database Basics',
  description: 'Fundamentals of HTTP, REST APIs, SQL, and database concepts every developer needs.',
  topics: ['HTTP', 'REST', 'SQL', 'Indexes', 'Transactions'],
  estimatedMinutes: 20,
  questions: [
    {
      id: 'fwd-001',
      topic: 'HTTP',
      difficulty: 'easy',
      prompt: 'What is the main difference between HTTP GET and POST?',
      options: [
        { id: 'a', text: 'GET is faster than POST' },
        { id: 'b', text: 'GET retrieves data without side effects; POST submits data and can change server state' },
        { id: 'c', text: 'GET requires authentication; POST does not' },
        { id: 'd', text: 'GET is only for HTML pages; POST is for APIs' },
      ],
      correctOptionId: 'b',
      definition: 'GET is a safe, idempotent HTTP method for retrieving data that should not cause side effects. POST submits data to the server, typically creating or processing a resource, and may change server state.',
      explanation: 'The distinction matters for caching (GET responses can be cached; POST responses typically cannot), browser behavior (navigating back to a GET page is safe; browsers warn before resubmitting POST), and REST API design. "Safe" means no side effects; "idempotent" means repeating the request has the same effect as doing it once.',
      whyOthersAreWrong: [
        'Option A: Performance is determined by payload, server logic, and network conditions — not by the HTTP method itself.',
        'Option C: Both GET and POST can require authentication or be unauthenticated depending on the endpoint; authentication is orthogonal to the method.',
        'Option D: Both GET and POST are used extensively for HTML pages and APIs; the distinction is about semantics and side effects, not the content type.',
      ],
      example: 'Fetching a user profile: `GET /users/123` — safe, cacheable, repeatable. Creating a user: `POST /users` with a JSON body — changes server state by inserting a row in the database. Submitting the same POST twice might create two users.',
    },
    {
      id: 'fwd-002',
      topic: 'HTTP',
      difficulty: 'easy',
      prompt: 'What does HTTP status code 404 mean?',
      options: [
        { id: 'a', text: 'Internal server error' },
        { id: 'b', text: 'The request was successful' },
        { id: 'c', text: 'The requested resource was not found' },
        { id: 'd', text: 'Access is forbidden' },
      ],
      correctOptionId: 'c',
      definition: 'HTTP 404 Not Found indicates that the server cannot locate the resource at the requested URL — either the resource never existed or has been removed.',
      explanation: 'HTTP status codes are grouped by prefix: 2xx = success, 3xx = redirect, 4xx = client error (the request is the problem), 5xx = server error. 404 is a 4xx error meaning the client asked for something the server cannot find. It is different from 403 Forbidden (you are not allowed) or 410 Gone (existed but was permanently deleted).',
      whyOthersAreWrong: [
        'Option A: Internal server error is 500 — a 5xx code indicating the server itself failed to handle the request.',
        'Option B: A successful request is indicated by 200 OK or other 2xx codes.',
        'Option D: Access forbidden is 403, indicating the server understands the request but refuses to authorize it.',
      ],
      example: 'Calling `GET /api/users/99999` when user 99999 does not exist returns `404 Not Found`. In an Express.js API: `if (!user) return res.status(404).json({ error: "User not found" });`.',
    },
    {
      id: 'fwd-003',
      topic: 'REST',
      difficulty: 'easy',
      prompt: 'What is REST?',
      options: [
        { id: 'a', text: 'A programming language for APIs' },
        { id: 'b', text: 'An architectural style for designing networked APIs using HTTP methods and stateless communication' },
        { id: 'c', text: 'A database query language' },
        { id: 'd', text: 'A framework for building web servers' },
      ],
      correctOptionId: 'b',
      definition: 'REST (Representational State Transfer) is an architectural style defined by Roy Fielding for building distributed systems that uses HTTP methods (GET, POST, PUT, DELETE) to operate on resources identified by URLs, with stateless request semantics.',
      explanation: 'REST\'s core constraints are: stateless (each request contains all information needed), uniform interface (resources identified by URL, manipulated via representations), and layered (clients do not need to know if they are connected directly to the server or a proxy). RESTful APIs are the most common pattern for web APIs because they leverage HTTP directly.',
      whyOthersAreWrong: [
        'Option A: REST is an architectural style, not a programming language. APIs can be built with REST principles in any language.',
        'Option C: SQL is the database query language; REST is an API design style that typically communicates over HTTP with JSON or XML.',
        'Option D: REST is not a framework; frameworks like Express, ASP.NET, or Fastify can be used to build RESTful APIs, but REST itself is just a set of architectural principles.',
      ],
      example: 'A RESTful user API: `GET /users` (list), `GET /users/1` (get one), `POST /users` (create), `PUT /users/1` (update), `DELETE /users/1` (delete). Each URL identifies a resource and the HTTP method defines the action.',
    },
    {
      id: 'fwd-004',
      topic: 'REST',
      difficulty: 'medium',
      prompt: 'What is idempotency in HTTP?',
      options: [
        { id: 'a', text: 'The ability to cache a request' },
        { id: 'b', text: 'A request that can be made multiple times with the same result — the server state changes only on the first call' },
        { id: 'c', text: 'The ability to compress request bodies' },
        { id: 'd', text: 'Sending the same request to multiple servers' },
      ],
      correctOptionId: 'b',
      definition: 'An idempotent HTTP operation is one where making the same request multiple times produces the same server state as making it once — subsequent identical requests have no additional effect.',
      explanation: 'GET, PUT, and DELETE are idempotent: calling `DELETE /users/1` repeatedly is safe — after the first call the user is gone, subsequent calls return 404 but change nothing further. POST is not idempotent: calling `POST /orders` twice creates two orders. Idempotency enables safe retries in unreliable networks.',
      whyOthersAreWrong: [
        'Option A: Cacheability is a separate HTTP property related to whether responses can be stored; GET is both cacheable and idempotent, but they are distinct concepts.',
        'Option C: Compression (gzip, br) is a transfer-encoding concern independent of idempotency.',
        'Option D: Sending to multiple servers describes fan-out or replication patterns, not the idempotency of an individual request.',
      ],
      example: 'Retrying a payment: if `POST /payments` is not idempotent, a network retry could charge the customer twice. The fix is to include a unique idempotency key: `POST /payments` with header `Idempotency-Key: uuid-abc123` — the server returns the cached result for duplicate keys.',
    },
    {
      id: 'fwd-005',
      topic: 'SQL',
      difficulty: 'easy',
      prompt: 'What is a foreign key?',
      options: [
        { id: 'a', text: 'A primary identifier that must be unique' },
        { id: 'b', text: 'A column in one table that references the primary key of another table, establishing a relationship' },
        { id: 'c', text: 'A key used to encrypt database columns' },
        { id: 'd', text: 'An index on a frequently queried column' },
      ],
      correctOptionId: 'b',
      definition: 'A foreign key is a column (or group of columns) in one table that references the primary key of another table, enforcing referential integrity between the two tables.',
      explanation: 'Foreign keys enforce that a referenced row must exist in the parent table. For example, an `orders.user_id` foreign key referencing `users.id` prevents inserting an order for a non-existent user, and optionally prevents deleting a user who still has orders. This constraint maintains data consistency at the database level.',
      whyOthersAreWrong: [
        'Option A: A primary identifier that must be unique is a primary key; foreign keys reference primary keys but are themselves allowed to be non-unique and nullable.',
        'Option C: Column encryption is a separate security feature; foreign keys are structural relationship constraints with no encryption properties.',
        'Option D: An index on a frequently queried column is simply called an index; foreign key columns often have indexes added for performance, but the foreign key constraint itself is about referential integrity.',
      ],
      example: 'SQL: `CREATE TABLE orders (id INT PRIMARY KEY, user_id INT REFERENCES users(id), total DECIMAL);`. Inserting an order with `user_id = 999` fails if no user with id 999 exists. The database enforces the relationship automatically.',
    },
    {
      id: 'fwd-006',
      topic: 'Indexes',
      difficulty: 'easy',
      prompt: 'What does a database index do?',
      options: [
        { id: 'a', text: 'Stores backups of table data' },
        { id: 'b', text: 'Speeds up data retrieval by creating a separate data structure that allows faster lookups' },
        { id: 'c', text: 'Enforces uniqueness on a column' },
        { id: 'd', text: 'Compresses the table to save space' },
      ],
      correctOptionId: 'b',
      definition: 'A database index is a separate data structure (typically a B-tree) that maintains a sorted copy of one or more columns, allowing the database engine to find matching rows in O(log n) instead of scanning the entire table.',
      explanation: 'Without an index, a query filtering on a column requires a full table scan — O(n). With an index, the database traverses the B-tree to find matching entries in O(log n) and retrieves only the relevant rows. Indexes dramatically speed up SELECT queries but add overhead to INSERT, UPDATE, and DELETE because the index structure must be maintained.',
      whyOthersAreWrong: [
        'Option A: Backups are stored by separate backup tools and processes; indexes are a query performance structure, not backup storage.',
        'Option C: A UNIQUE index does enforce uniqueness, but a regular index does not. The primary purpose of an index is query performance; uniqueness enforcement is an optional additional constraint.',
        'Option D: Indexes use additional disk space — they do not compress data. A table with many indexes uses more storage, not less.',
      ],
      example: 'Without index: `SELECT * FROM users WHERE email = "alice@example.com"` scans all 10 million rows. With `CREATE INDEX idx_users_email ON users(email)`, the query jumps directly to the matching rows in milliseconds.',
    },
    {
      id: 'fwd-007',
      topic: 'SQL',
      difficulty: 'medium',
      prompt: 'What does a SQL JOIN do?',
      options: [
        { id: 'a', text: 'Merges two databases into one' },
        { id: 'b', text: 'Combines rows from two or more tables based on a related column' },
        { id: 'c', text: 'Deletes duplicate rows from a table' },
        { id: 'd', text: 'Creates a new table from query results' },
      ],
      correctOptionId: 'b',
      definition: 'A SQL JOIN combines rows from two or more tables based on a matching condition (usually a shared key), returning a result set that includes columns from all joined tables.',
      explanation: 'INNER JOIN returns only rows with matches in both tables. LEFT JOIN returns all rows from the left table and matching rows from the right (nulls for no match). This allows querying across related tables without denormalizing data. JOINs are fundamental to relational databases designed with normalized schemas.',
      whyOthersAreWrong: [
        'Option A: Merging databases is a separate administrative operation (data migration); JOIN operates within a single database query.',
        'Option C: Removing duplicate rows is done with SELECT DISTINCT or DELETE with a ROW_NUMBER() pattern; JOIN combines tables regardless of duplicates.',
        'Option D: Creating a new table from results is done with `CREATE TABLE AS SELECT ...`; a JOIN just produces a result set within the query.',
      ],
      example: '`SELECT u.name, o.total FROM users u INNER JOIN orders o ON u.id = o.user_id WHERE u.id = 5;` — returns the name of user 5 combined with each of their orders. Without JOIN, you would need two separate queries.',
    },
    {
      id: 'fwd-008',
      topic: 'Transactions',
      difficulty: 'medium',
      prompt: 'What does ACID stand for in database transactions?',
      options: [
        { id: 'a', text: 'Atomicity, Consistency, Isolation, Durability' },
        { id: 'b', text: 'Async, Concurrent, Indexed, Durable' },
        { id: 'c', text: 'Atomicity, Concurrency, Integrity, Decoupled' },
        { id: 'd', text: 'Available, Consistent, Independent, Distributed' },
      ],
      correctOptionId: 'a',
      definition: 'ACID is a set of four properties that guarantee database transactions are processed reliably: Atomicity (all-or-nothing), Consistency (data remains valid), Isolation (concurrent transactions do not interfere), and Durability (committed data survives crashes).',
      explanation: 'Atomicity ensures that a transfer that debits one account and credits another either completes fully or not at all. Consistency ensures business rules (like balance cannot go negative) are never violated. Isolation prevents two concurrent transactions from seeing each other\'s intermediate state. Durability ensures committed data is not lost even after a power failure.',
      whyOthersAreWrong: [
        'Option B: "Async, Concurrent, Indexed, Durable" is not a real acronym; ACID properties are about transactional guarantees, not about async programming or indexing.',
        'Option C: "Atomicity, Concurrency, Integrity, Decoupled" is not the correct expansion; the C is Consistency and the I is Isolation.',
        'Option D: "Available, Consistent, Independent, Distributed" conflates ACID with CAP theorem terminology (Available, Consistent, Partition-tolerant).',
      ],
      example: 'Bank transfer: `BEGIN; UPDATE accounts SET balance = balance - 100 WHERE id = 1; UPDATE accounts SET balance = balance + 100 WHERE id = 2; COMMIT;`. If the server crashes between the two UPDATEs, atomicity rolls back the entire transaction so no money is lost or created.',
    },
    {
      id: 'fwd-009',
      topic: 'HTTP',
      difficulty: 'medium',
      prompt: 'What is CORS?',
      options: [
        { id: 'a', text: 'A caching strategy for static assets' },
        { id: 'b', text: 'A browser security mechanism that controls which origins can make requests to a server' },
        { id: 'c', text: 'A protocol for encrypting HTTP traffic' },
        { id: 'd', text: 'A method of compressing API responses' },
      ],
      correctOptionId: 'b',
      definition: 'CORS (Cross-Origin Resource Sharing) is a browser security mechanism that uses HTTP headers to allow or block web pages from making requests to a different origin (domain, protocol, or port) than the one that served the page.',
      explanation: 'Browsers enforce the Same-Origin Policy by default, blocking JavaScript from reading responses from different origins. CORS allows servers to opt in to cross-origin requests by including headers like `Access-Control-Allow-Origin`. Without CORS, a malicious website could not use JavaScript to read your data from another site you are logged into.',
      whyOthersAreWrong: [
        'Option A: Caching strategies involve Cache-Control, ETags, and CDNs; CORS is about cross-origin access control, not caching.',
        'Option C: Encrypting HTTP traffic is HTTPS (TLS); CORS is a same-origin policy relaxation mechanism layered on top of HTTP.',
        'Option D: Response compression uses Accept-Encoding/Content-Encoding headers (gzip, br); CORS is about origin-based access control.',
      ],
      example: 'A React app on `https://app.example.com` calls `https://api.example.com/users`. The browser sends a preflight `OPTIONS` request. The API responds with `Access-Control-Allow-Origin: https://app.example.com`, allowing the browser to proceed. Without this header, the browser blocks the response.',
    },
    {
      id: 'fwd-010',
      topic: 'HTTP',
      difficulty: 'easy',
      prompt: 'What is the difference between a cookie and a session?',
      options: [
        { id: 'a', text: 'Cookies are stored server-side; sessions are stored client-side' },
        { id: 'b', text: 'Cookies are small data stored in the browser; a session is server-side state often identified by a cookie' },
        { id: 'c', text: 'Sessions are always encrypted; cookies never are' },
        { id: 'd', text: 'There is no practical difference' },
      ],
      correctOptionId: 'b',
      definition: 'A cookie is a small piece of data stored in the browser and sent with every request to the matching domain. A session is server-side state (stored in memory, Redis, or a database) that is tied to a client via a session ID typically stored in a cookie.',
      explanation: 'Cookies persist on the client and are visible (unless HttpOnly). Sessions store the actual data server-side, with the client holding only an opaque session ID. This keeps sensitive data off the client. Sessions expire when the server deletes the session data or the cookie expires.',
      whyOthersAreWrong: [
        'Option A: This is backwards — cookies are client-side; session data is server-side.',
        'Option C: Cookies can be encrypted (via Secure flag and server-side signing), and sessions stored server-side can also have security issues if the session ID is stolen.',
        'Option D: There is a meaningful architectural difference: cookies are client-side state; sessions are server-side state linked by a cookie.',
      ],
      example: 'After login, the server creates a session (stores `{ userId: 42, role: "admin" }` in Redis), generates a session ID, and sends `Set-Cookie: sessionId=abc123; HttpOnly`. The browser sends `Cookie: sessionId=abc123` on every subsequent request, and the server looks up the session data.',
    },
    {
      id: 'fwd-011',
      topic: 'REST',
      difficulty: 'easy',
      prompt: 'What is a query parameter in a URL?',
      options: [
        { id: 'a', text: 'A value embedded in the URL path' },
        { id: 'b', text: 'A key-value pair appended to a URL after a ? character, used to filter or customize a request' },
        { id: 'c', text: 'The HTTP method used in the request' },
        { id: 'd', text: 'A header sent with the request' },
      ],
      correctOptionId: 'b',
      definition: 'A query parameter is a key-value pair appended to a URL after a `?` character, used to pass optional filtering, sorting, pagination, or configuration data to an endpoint.',
      explanation: 'Multiple query parameters are separated by `&`. They are used for optional data that modifies how the resource is returned (filtering, sorting, pagination) rather than identifying the resource itself (which belongs in the path). Query parameters are visible in the URL and should not be used for sensitive data.',
      whyOthersAreWrong: [
        'Option A: A value embedded in the URL path is a path parameter (e.g., `/users/:id`); path parameters identify specific resources.',
        'Option C: The HTTP method (GET, POST, etc.) is specified in the request line, not in the URL itself.',
        'Option D: A header is a key-value pair in the HTTP request envelope, separate from the URL; headers carry metadata like Content-Type and Authorization.',
      ],
      example: '`GET /products?category=electronics&sort=price&page=2` — `category=electronics`, `sort=price`, and `page=2` are query parameters. They filter and paginate the `/products` resource. In Express: `req.query.category // "electronics"`.',
    },
    {
      id: 'fwd-012',
      topic: 'HTTP',
      difficulty: 'easy',
      prompt: 'What does HTTP status code 500 indicate?',
      options: [
        { id: 'a', text: 'The request was redirected' },
        { id: 'b', text: 'The client sent a bad request' },
        { id: 'c', text: 'An unexpected error occurred on the server' },
        { id: 'd', text: 'Authentication is required' },
      ],
      correctOptionId: 'c',
      definition: 'HTTP 500 Internal Server Error indicates that the server encountered an unexpected condition that prevented it from fulfilling the request — a server-side bug or unhandled exception.',
      explanation: '5xx codes represent server errors where the client\'s request may have been valid but the server failed to process it. 500 is the generic server error. Other 5xx codes include 502 Bad Gateway (upstream server issue), 503 Service Unavailable (overloaded or down), and 504 Gateway Timeout. A 500 from a production API should trigger alerts and investigation.',
      whyOthersAreWrong: [
        'Option A: Redirects are 3xx codes (301 Moved Permanently, 302 Found); they tell the client to make a new request to a different URL.',
        'Option B: Client-caused bad requests are 4xx codes — 400 Bad Request specifically means the request was malformed.',
        'Option D: Authentication required is 401 Unauthorized; 403 Forbidden means authenticated but not authorized.',
      ],
      example: 'In an Express.js API, an unhandled exception in a route handler might bubble up and result in a 500 response. Proper error handling middleware catches such exceptions: `app.use((err, req, res, next) => { console.error(err); res.status(500).json({ error: "Internal server error" }); });`.',
    },
    {
      id: 'fwd-013',
      topic: 'REST',
      difficulty: 'easy',
      prompt: 'What is JSON?',
      options: [
        { id: 'a', text: 'A JavaScript-only data format' },
        { id: 'b', text: 'A lightweight, text-based data-interchange format that is easy for humans to read and for machines to parse' },
        { id: 'c', text: 'A binary serialization format' },
        { id: 'd', text: 'A database storage format' },
      ],
      correctOptionId: 'b',
      definition: 'JSON (JavaScript Object Notation) is a lightweight, human-readable text format for representing structured data using key-value pairs, arrays, strings, numbers, booleans, and null.',
      explanation: 'Despite its name, JSON is language-independent — every mainstream language has libraries to serialize and deserialize JSON. It is the dominant format for REST API request and response bodies because it is simpler than XML, readable by humans, and natively parsed by JavaScript in browsers. JSON supports nesting to arbitrary depth.',
      whyOthersAreWrong: [
        'Option A: JSON originated from JavaScript syntax but is completely language-independent. Python, C#, Java, Go, and virtually every language can read and write JSON.',
        'Option C: JSON is text-based (UTF-8); binary serialization formats include Protocol Buffers and MessagePack which are more compact but not human-readable.',
        'Option D: JSON is a data exchange format; while some databases like PostgreSQL support JSON columns, JSON itself is not a database storage format.',
      ],
      example: '`{ "userId": 42, "name": "Alice", "roles": ["admin", "editor"], "active": true }`. In TypeScript: `const obj = JSON.parse(jsonString);` and `const json = JSON.stringify(obj);`. The `fetch` API returns JSON bodies via `await res.json()`.',
    },
    {
      id: 'fwd-014',
      topic: 'SQL',
      difficulty: 'easy',
      prompt: 'What is the main difference between SQL and NoSQL databases?',
      options: [
        { id: 'a', text: 'SQL databases are faster' },
        { id: 'b', text: 'SQL databases use structured tables and a fixed schema; NoSQL databases use flexible document, key-value, or graph models' },
        { id: 'c', text: 'NoSQL supports transactions but SQL does not' },
        { id: 'd', text: 'SQL can only store text; NoSQL stores all types' },
      ],
      correctOptionId: 'b',
      definition: 'SQL (relational) databases organize data into structured tables with a predefined schema and use SQL for queries. NoSQL databases use flexible data models (documents, key-value, column-family, or graphs) that do not require a fixed schema.',
      explanation: 'SQL databases excel at complex queries, relationships between entities, and transactional integrity (ACID). NoSQL databases excel at horizontal scalability, flexible schemas that can evolve quickly, and specific access patterns (e.g., Redis for fast key lookups, MongoDB for document storage, Neo4j for graph traversal). The best choice depends on the use case.',
      whyOthersAreWrong: [
        'Option A: Performance depends on use case, indexing, and query patterns — neither category is universally faster.',
        'Option C: The opposite is closer to truth: SQL (relational) databases have strong ACID transaction support; many NoSQL databases trade full ACID for scalability, though modern NoSQL systems increasingly support transactions.',
        'Option D: SQL databases store all data types (text, numbers, dates, JSON, binary blobs); this claim is incorrect.',
      ],
      example: 'E-commerce order history with complex JOIN queries → PostgreSQL (SQL). User session storage where you need millisecond key lookups → Redis (NoSQL key-value). Social network relationships → Neo4j (NoSQL graph). Content management with variable document structures → MongoDB (NoSQL document).',
    },
    {
      id: 'fwd-015',
      topic: 'REST',
      difficulty: 'easy',
      prompt: 'What is an API endpoint?',
      options: [
        { id: 'a', text: 'The server hardware hosting the API' },
        { id: 'b', text: 'A specific URL at which an API can be accessed to perform a particular operation' },
        { id: 'c', text: 'The authentication token for an API' },
        { id: 'd', text: 'The database table behind an API route' },
      ],
      correctOptionId: 'b',
      definition: 'An API endpoint is a specific URL combined with an HTTP method that defines a point of interaction with an API — each endpoint maps to a particular operation or resource.',
      explanation: 'Endpoints are the interface contract between a client and a server. Each endpoint has a URL path that identifies the resource and an HTTP method that defines the action. Good REST API design makes endpoints predictable and self-describing — `/users/{id}` clearly refers to a specific user resource.',
      whyOthersAreWrong: [
        'Option A: Server hardware is infrastructure; endpoints are logical URL addresses, not physical machines.',
        'Option C: Authentication tokens are credentials used to authenticate requests; they are sent as headers or query parameters, not the endpoint itself.',
        'Option D: Database tables are implementation details; an endpoint is an external interface that may be backed by one or many tables (or no database at all).',
      ],
      example: 'A user API has endpoints: `GET /users` (list users), `POST /users` (create user), `GET /users/:id` (get user), `PATCH /users/:id` (update), `DELETE /users/:id` (delete). Each combination of URL + method is a distinct endpoint with a defined contract.',
    },
  ],
};
