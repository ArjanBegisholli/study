import type { Test } from '../../types/quiz';

export const foundationCodeQuality: Test = {
  id: 'foundation-code-quality',
  role: 'foundation',
  title: 'Language & Code Quality',
  description: 'Writing clean, maintainable code: principles, patterns, and practices every developer should follow.',
  topics: ['Clean Code', 'Naming', 'Modularity', 'Readability', 'Error Handling'],
  estimatedMinutes: 20,
  questions: [
    {
      id: 'fcq-001',
      topic: 'Clean Code',
      difficulty: 'easy',
      prompt: 'What does the DRY principle stand for?',
      options: [
        { id: 'a', text: 'Do Repeat Yourself' },
        { id: 'b', text: "Don't Repeat Yourself" },
        { id: 'c', text: 'Dynamic Runtime Yield' },
        { id: 'd', text: 'Direct Resource Yielding' },
      ],
      correctOptionId: 'b',
      definition: "DRY (Don't Repeat Yourself) states that every piece of knowledge or logic should have a single, authoritative representation in the codebase.",
      explanation: 'When you duplicate logic, every future change must be applied in multiple places. Missing one creates inconsistency and bugs. DRY encourages extracting repeated logic into functions, modules, or shared utilities. Note: DRY is about knowledge duplication, not just syntactic duplication — sometimes similar-looking code represents different concepts and should not be merged.',
      whyOthersAreWrong: [
        'Option A: "Do Repeat Yourself" is the opposite of the principle — repeating yourself is exactly what DRY tells you to avoid.',
        'Option C: "Dynamic Runtime Yield" is not a real software engineering term.',
        'Option D: "Direct Resource Yielding" is not a real software engineering term.',
      ],
      example: 'If you calculate a user\'s discount in three places in your codebase, extract it: `function calculateDiscount(price: number, tier: string): number { ... }`. Now a rule change requires editing one place only.',
    },
    {
      id: 'fcq-002',
      topic: 'Modularity',
      difficulty: 'easy',
      prompt: 'What does the Single Responsibility Principle (SRP) state?',
      options: [
        { id: 'a', text: 'A class should do as many things as possible' },
        { id: 'b', text: 'A class or function should have only one reason to change' },
        { id: 'c', text: 'Every function must be a single line' },
        { id: 'd', text: 'A module should import only one dependency' },
      ],
      correctOptionId: 'b',
      definition: 'The Single Responsibility Principle states that a class or module should have exactly one reason to change, meaning it should encapsulate one cohesive area of functionality.',
      explanation: 'If a class changes for multiple unrelated reasons — say, both when the business rules change and when the database schema changes — it has too many responsibilities. Separating these makes each class smaller, easier to test, and less likely to introduce bugs when one concern changes. SRP is the "S" in SOLID.',
      whyOthersAreWrong: [
        'Option A: A class doing as many things as possible is a "God class" — the anti-pattern SRP is designed to prevent.',
        'Option C: SRP says nothing about function length; a single responsibility can require many lines of well-structured code.',
        'Option D: The number of imports is unrelated to responsibility; a module with one responsibility may legitimately import many libraries.',
      ],
      example: 'A `UserService` that handles authentication, sends welcome emails, and writes to the database violates SRP. Split into `AuthService`, `EmailService`, and `UserRepository` — each changes independently when its concern changes.',
    },
    {
      id: 'fcq-003',
      topic: 'Clean Code',
      difficulty: 'easy',
      prompt: 'What is a "code smell"?',
      options: [
        { id: 'a', text: 'A security vulnerability in source code' },
        { id: 'b', text: 'An indicator in the code that suggests a deeper problem or design issue' },
        { id: 'c', text: 'Code that produces incorrect output' },
        { id: 'd', text: 'Unused imports in a file' },
      ],
      correctOptionId: 'b',
      definition: 'A code smell is a surface-level characteristic of code that may indicate a deeper design or quality problem, without necessarily being a bug in itself.',
      explanation: 'Code smells do not prevent software from working but they make it harder to maintain, understand, and extend. Common smells include long methods, large classes, duplicate code, feature envy (a method that uses another class\'s data more than its own), and deep nesting. Identifying and addressing smells is a key part of refactoring.',
      whyOthersAreWrong: [
        'Option A: Security vulnerabilities are a specific, severe category of defect; code smells are broader design indicators and are not necessarily security-related.',
        'Option C: Code smells can exist in perfectly functioning code; the concern is maintainability and design quality, not correctness.',
        'Option D: Unused imports are a specific linter warning; while they could indicate a smell, the term "code smell" encompasses a much broader range of design issues.',
      ],
      example: 'A function with 300 lines and 10 parameters is a code smell ("long method" and "long parameter list"). It suggests the function has too many responsibilities. The fix is to extract smaller, focused functions — even though the 300-line version might work correctly.',
    },
    {
      id: 'fcq-004',
      topic: 'Readability',
      difficulty: 'medium',
      prompt: "What is cyclomatic complexity?",
      options: [
        { id: 'a', text: 'The number of classes in a module' },
        { id: 'b', text: "A measure of the number of linearly independent paths through a function's source code" },
        { id: 'c', text: 'The depth of nested loops in a program' },
        { id: 'd', text: 'The ratio of comments to code lines' },
      ],
      correctOptionId: 'b',
      definition: "Cyclomatic complexity is a software metric introduced by Thomas McCabe that counts the number of linearly independent paths through a function's control flow graph.",
      explanation: 'Each `if`, `else if`, `for`, `while`, `case`, `&&`, and `||` adds one to cyclomatic complexity. Higher complexity means more paths to test and harder-to-read code. A function with complexity > 10 is often considered too complex and a candidate for refactoring. Linters like ESLint can enforce a maximum cyclomatic complexity.',
      whyOthersAreWrong: [
        'Option A: The number of classes is a module-level metric; cyclomatic complexity is measured at the function level.',
        'Option C: Nesting depth is a related but distinct metric; cyclomatic complexity counts decision branches, not nesting levels specifically.',
        'Option D: Comment-to-code ratio is a documentation metric; cyclomatic complexity is purely about control flow branching.',
      ],
      example: 'A function with no branches has a cyclomatic complexity of 1. Adding one `if` makes it 2, adding a `for` loop makes it 3. A function with complexity 15 has 15 different paths — you would need 15 test cases for full branch coverage.',
    },
    {
      id: 'fcq-005',
      topic: 'Readability',
      difficulty: 'easy',
      prompt: 'Why should you avoid magic numbers in code?',
      options: [
        { id: 'a', text: 'They make the code run slower' },
        { id: 'b', text: 'They are hard to read and maintain because their meaning is not obvious without context' },
        { id: 'c', text: 'They cause overflow errors' },
        { id: 'd', text: 'They are not supported in all languages' },
      ],
      correctOptionId: 'b',
      definition: 'A magic number is an unnamed numeric constant used directly in code without explanation. Replacing magic numbers with named constants makes the code self-documenting.',
      explanation: 'When you see `if (status === 3)` you cannot know what 3 means. When you see `if (status === OrderStatus.Shipped)` the intent is immediately clear. Named constants also centralize changes — if the value needs to change, you update it in one place instead of searching for all occurrences of the raw number.',
      whyOthersAreWrong: [
        'Option A: Magic numbers have no effect on execution speed; they are a readability and maintainability problem, not a performance one.',
        'Option C: Numeric overflow depends on the value and type, not on whether a number is named or unnamed.',
        'Option D: All mainstream languages support numeric literals; this is not a language compatibility issue.',
      ],
      example: 'Bad: `setTimeout(callback, 86400000);`. Good: `const ONE_DAY_MS = 24 * 60 * 60 * 1000; setTimeout(callback, ONE_DAY_MS);`. The named constant explains the intent, and changing one day to two days requires only one edit.',
    },
    {
      id: 'fcq-006',
      topic: 'Clean Code',
      difficulty: 'medium',
      prompt: 'What is defensive programming?',
      options: [
        { id: 'a', text: 'Writing code that protects against unauthorized access' },
        { id: 'b', text: 'Writing code that anticipates and handles invalid inputs and unexpected states gracefully' },
        { id: 'c', text: 'Adding encryption to sensitive methods' },
        { id: 'd', text: 'Avoiding the use of third-party libraries' },
      ],
      correctOptionId: 'b',
      definition: 'Defensive programming is a practice of writing code that expects and handles errors, edge cases, and invalid inputs rather than assuming everything will always go as expected.',
      explanation: 'Defensive code validates inputs at function boundaries, checks for null/undefined before dereferencing, handles unexpected enum values with default cases, and fails fast with informative error messages. It improves robustness and makes bugs easier to diagnose. It contrasts with trusting all callers to pass valid inputs.',
      whyOthersAreWrong: [
        'Option A: Protecting against unauthorized access is a security concern (authentication and authorization), which overlaps with but is distinct from defensive programming.',
        'Option C: Encryption is a security technique; defensive programming is about correctness and robustness under unexpected conditions.',
        'Option D: Avoiding third-party libraries is a dependency management decision unrelated to handling invalid inputs.',
      ],
      example: 'Instead of `function getUser(id: string) { return db.findById(id); }`, a defensive version validates first: `if (!id || typeof id !== "string") throw new Error("Invalid id"); const user = await db.findById(id); if (!user) throw new NotFoundError(id); return user;`.',
    },
    {
      id: 'fcq-007',
      topic: 'Readability',
      difficulty: 'easy',
      prompt: 'When should you add a code comment?',
      options: [
        { id: 'a', text: 'On every line of code' },
        { id: 'b', text: 'When the code cannot clearly express why something is done, not what it does' },
        { id: 'c', text: 'Only at the top of each file' },
        { id: 'd', text: 'Whenever a variable is declared' },
      ],
      correctOptionId: 'b',
      definition: 'Good comments explain the "why" — the intent, the business reason, or a non-obvious constraint — not the "what", which should be clear from well-named code.',
      explanation: 'The code already shows what is being done; a comment that says `// increment i by 1` above `i++` adds no value. Comments that explain business rules, trade-off decisions, or known quirks are invaluable. Over-commenting is also a problem — it creates maintenance burden when code changes but comments are not updated.',
      whyOthersAreWrong: [
        'Option A: Commenting every line is noise that obscures the code and becomes a maintenance burden when code changes.',
        'Option C: File-level comments (e.g., describing the module\'s purpose) are useful, but limiting comments to only the top of files means missing explanations of non-obvious inline logic.',
        'Option D: Variable declarations should be self-explanatory through naming; adding a comment to every declaration is redundant and noisy.',
      ],
      example: '// Bad: `i++; // increment i` — the code already says this.\n// Good: `// Workaround for Safari bug #12345: must delay by 100ms before reading offsetWidth\nsetTimeout(() => { width = el.offsetWidth; }, 100);`',
    },
    {
      id: 'fcq-008',
      topic: 'Clean Code',
      difficulty: 'easy',
      prompt: 'What is an early return (guard clause)?',
      options: [
        { id: 'a', text: 'A return statement at the end of a function' },
        { id: 'b', text: 'Returning from a function immediately when a condition is met, reducing nesting' },
        { id: 'c', text: 'A way to skip the first iteration of a loop' },
        { id: 'd', text: 'A try-catch block at the start of a function' },
      ],
      correctOptionId: 'b',
      definition: 'A guard clause is an early return at the top of a function that handles edge cases or invalid inputs immediately, reducing the nesting depth of the happy path.',
      explanation: 'Deeply nested code with many `if-else` blocks is hard to read. Guard clauses invert the condition and return early, leaving the main logic at the lowest nesting level. This makes functions easier to read top-to-bottom because each guard handles a specific precondition failure before the main logic begins.',
      whyOthersAreWrong: [
        'Option A: A return at the end is the normal return statement in a function — not a guard clause.',
        'Option C: Skipping the first loop iteration is done with a `continue` statement (with a condition) or by starting the loop at index 1.',
        'Option D: A try-catch block at the start is error handling infrastructure; a guard clause is specifically an early conditional return.',
      ],
      example: 'Without guard clauses: `function process(user) { if (user) { if (user.isActive) { if (user.role === "admin") { doAdminWork(); } } } }`. With guard clauses: `function process(user) { if (!user) return; if (!user.isActive) return; if (user.role !== "admin") return; doAdminWork(); }`.',
    },
    {
      id: 'fcq-009',
      topic: 'Clean Code',
      difficulty: 'easy',
      prompt: 'What does a linter do?',
      options: [
        { id: 'a', text: 'Compresses code into a smaller file' },
        { id: 'b', text: 'Automatically analyzes source code for potential errors, style violations, and bad practices' },
        { id: 'c', text: 'Runs automated tests against the codebase' },
        { id: 'd', text: 'Converts TypeScript to JavaScript' },
      ],
      correctOptionId: 'b',
      definition: 'A linter is a static analysis tool that reads source code without executing it to detect bugs, enforce style rules, flag suspicious constructs, and highlight deviations from best practices.',
      explanation: 'Linters catch issues before tests even run: undefined variables, unreachable code, unused imports, overly complex functions, potential null dereferences, and style inconsistencies. Common linters include ESLint for JavaScript/TypeScript and ReSharper for C#. Many teams enforce linting in CI to prevent rule violations from merging.',
      whyOthersAreWrong: [
        'Option A: Compressing code (minification) is done by bundlers like Webpack or esbuild, not linters.',
        'Option C: Running automated tests is done by test runners like Jest or Vitest; linters perform static analysis without executing code.',
        'Option D: Transpiling TypeScript to JavaScript is done by the TypeScript compiler (tsc); linting is a separate analysis step.',
      ],
      example: 'ESLint with the rule `no-unused-vars` will flag `const unused = 5;` if `unused` is never referenced. The `@typescript-eslint/no-floating-promises` rule flags async functions whose returned promises are not awaited, preventing silent failures.',
    },
    {
      id: 'fcq-010',
      topic: 'Clean Code',
      difficulty: 'easy',
      prompt: 'What is dead code?',
      options: [
        { id: 'a', text: 'Code that throws an unhandled exception' },
        { id: 'b', text: 'Code that is never executed because it is unreachable or unused' },
        { id: 'c', text: 'Code with no comments' },
        { id: 'd', text: 'Legacy code from a previous version' },
      ],
      correctOptionId: 'b',
      definition: 'Dead code is code that can never be executed — either because control flow can never reach it, or because it is defined but never called anywhere.',
      explanation: 'Dead code adds clutter, confuses developers reading it, and wastes maintenance effort. Common examples include code after a `return` statement, functions that are never called, and feature flags that are permanently set. It should be deleted, not just commented out. Version control preserves history if it is ever needed again.',
      whyOthersAreWrong: [
        'Option A: Code that throws an unhandled exception is executed — it just fails. That is a bug, not dead code.',
        'Option C: Absence of comments is a readability concern; it has nothing to do with whether code is executed.',
        'Option D: Legacy code refers to old code that is hard to maintain; it may still be actively executed and is not inherently dead.',
      ],
      example: '`function helper() { return true; const unreachable = false; }` — `unreachable` is dead code because the `return` on the previous line exits the function. TypeScript\'s compiler and ESLint will both flag this with `no-unreachable`.',
    },
    {
      id: 'fcq-011',
      topic: 'Naming',
      difficulty: 'easy',
      prompt: 'What does "naming should reveal intent" mean?',
      options: [
        { id: 'a', text: 'Variable names should be as short as possible' },
        { id: 'b', text: 'Names should clearly communicate the purpose or meaning of a variable, function, or class' },
        { id: 'c', text: 'All names should follow a strict format like Hungarian notation' },
        { id: 'd', text: 'Names should include the data type' },
      ],
      correctOptionId: 'b',
      definition: 'Naming should reveal intent means choosing names that make code self-explanatory — a reader can understand what a variable holds or a function does without reading the implementation.',
      explanation: 'Names like `d` or `x` force the reader to trace the code to understand what is stored. Names like `elapsedDaysInTrial` or `isUserAuthenticated` communicate meaning immediately. Good naming is the most cost-effective form of documentation because it lives in the code and cannot become stale.',
      whyOthersAreWrong: [
        'Option A: Short names like `i` are acceptable for loop counters by convention, but "short as possible" as a general rule sacrifices readability.',
        'Option C: Hungarian notation (encoding type in the name, e.g., `strName`) is a specific style that is now mostly discouraged in modern codebases; revealing intent is about meaning, not format.',
        'Option D: Including data types in names (like Hungarian notation) is largely unnecessary in typed languages where the compiler enforces types.',
      ],
      example: 'Bad: `const d = new Date(); const x = u.a;`. Good: `const registrationDate = new Date(); const emailAddress = user.email;`. The good versions communicate what the data represents without needing a comment.',
    },
    {
      id: 'fcq-012',
      topic: 'Modularity',
      difficulty: 'medium',
      prompt: 'What is a "God class"?',
      options: [
        { id: 'a', text: 'A class that is inherited by all other classes' },
        { id: 'b', text: 'A class that knows too much or does too much, violating the Single Responsibility Principle' },
        { id: 'c', text: 'A utility class with only static methods' },
        { id: 'd', text: 'The main entry point class of an application' },
      ],
      correctOptionId: 'b',
      definition: 'A God class is an anti-pattern where a single class accumulates excessive responsibilities, knowledge, and behavior — it "knows too much" about the system and becomes a bottleneck for changes.',
      explanation: 'God classes typically grow over time as developers add functionality to the most convenient place. They become hard to test (they depend on everything), hard to change (everything depends on them), and hard to understand (they span many unrelated concerns). The fix is to decompose them into smaller, focused classes with clear responsibilities.',
      whyOthersAreWrong: [
        'Option A: A base class inherited by all others is an architectural decision (like a common base entity); that is not what God class means.',
        'Option C: A utility class with static methods (like a math helper) is a legitimate pattern; a God class is characterized by excessive responsibility, not its method type.',
        'Option D: Application entry points like `Program.cs` or `index.ts` are conventionally minimal — they bootstrap the app; a God class is defined by accumulated logic, not entry-point status.',
      ],
      example: 'An `ApplicationManager` class that handles user authentication, sends emails, processes payments, manages caching, and generates reports is a God class. Each of those responsibilities should live in its own service class.',
    },
    {
      id: 'fcq-013',
      topic: 'Clean Code',
      difficulty: 'easy',
      prompt: 'What does YAGNI stand for?',
      options: [
        { id: 'a', text: 'You Always Get Needed Implementations' },
        { id: 'b', text: "You Aren't Gonna Need It — don't add functionality until it's required" },
        { id: 'c', text: 'Your Application Gets Noticeably Improved' },
        { id: 'd', text: 'You Always Generate New Interfaces' },
      ],
      correctOptionId: 'b',
      definition: "YAGNI (You Aren't Gonna Need It) is an Extreme Programming principle that says you should not add functionality until it is actually needed.",
      explanation: 'Developers often add features "just in case" they might be needed later. This speculative generality adds complexity, takes time to build and maintain, and is often wrong about what will actually be needed. YAGNI encourages building only what is required right now, keeping the codebase lean and focused. It pairs well with refactoring — add structure when you actually need it.',
      whyOthersAreWrong: [
        'Option A: "You Always Get Needed Implementations" is not a real principle and contradicts YAGNI.',
        'Option C: "Your Application Gets Noticeably Improved" is not a real principle.',
        'Option D: "You Always Generate New Interfaces" is not a real principle.',
      ],
      example: 'Building a plugin system before you have a single plugin, or adding multi-tenancy before your app has one tenant, violates YAGNI. Build the simplest thing that works now. Add the abstraction when the second use case arrives (then it\'s no longer speculative).',
    },
    {
      id: 'fcq-014',
      topic: 'Clean Code',
      difficulty: 'medium',
      prompt: 'Why are pure functions easier to test?',
      options: [
        { id: 'a', text: 'They run faster in test environments' },
        { id: 'b', text: 'They have no side effects and always return the same output for the same input, making them predictable' },
        { id: 'c', text: "They don't require any imports" },
        { id: 'd', text: 'They never throw exceptions' },
      ],
      correctOptionId: 'b',
      definition: 'Pure functions are deterministic and side-effect-free, so tests only need to verify that the correct output is returned for a given input — no mocks, stubs, or environment setup required.',
      explanation: 'Testing impure functions is hard because you need to set up and tear down external state (databases, file systems, network services). A pure function test is just: arrange inputs, call function, assert output. This makes tests fast, isolated, and deterministic. Structuring code to maximize pure functions and push side effects to the boundaries is a core clean architecture goal.',
      whyOthersAreWrong: [
        'Option A: Execution speed is not affected by purity; a pure function could be slower than an impure one.',
        "Option C: Whether a function imports modules is unrelated to its purity; pure functions can and do import dependencies.",
        'Option D: Pure functions can throw exceptions for invalid inputs; purity is about determinism and side effects, not exception-freedom.',
      ],
      example: 'Testing `calculateTax(income: number, rate: number): number` requires just one line: `expect(calculateTax(100, 0.2)).toBe(20)`. No database, no HTTP mock. Compare to testing `saveUserAndSendEmail(user)` which requires mocking the DB and email service.',
    },
    {
      id: 'fcq-015',
      topic: 'Clean Code',
      difficulty: 'easy',
      prompt: 'What is refactoring?',
      options: [
        { id: 'a', text: 'Rewriting the entire codebase from scratch' },
        { id: 'b', text: "Improving the internal structure of code without changing its external behavior" },
        { id: 'c', text: 'Adding new features to existing code' },
        { id: 'd', text: 'Fixing bugs introduced in the last release' },
      ],
      correctOptionId: 'b',
      definition: "Refactoring is the process of restructuring existing code — improving its readability, reducing complexity, removing duplication — without altering what the code does externally.",
      explanation: 'Refactoring is not rewriting. The external behavior (the observable outputs) must remain identical; only the internal structure changes. This is verified by keeping the test suite green throughout. Common refactorings include extracting methods, renaming variables, removing duplication, and simplifying conditionals. Martin Fowler\'s "Refactoring" is the canonical reference.',
      whyOthersAreWrong: [
        'Option A: A full rewrite replaces the codebase entirely; refactoring makes incremental improvements to existing code while preserving behavior.',
        'Option C: Adding new features changes what the code does externally; refactoring explicitly must not change observable behavior.',
        'Option D: Fixing bugs changes code behavior (from incorrect to correct); refactoring preserves behavior.',
      ],
      example: 'Renaming `function calc(a, b) { return a + b; }` to `function addPrices(basePrice: number, tax: number): number { return basePrice + tax; }` is refactoring — the behavior is identical but the intent is now clear. The tests still pass.',
    },
  ],
};
