import type { Test } from '../../types/quiz';

export const foundationFundamentals: Test = {
  id: 'foundation-fundamentals',
  role: 'foundation',
  title: 'Programming Fundamentals',
  description: 'Core concepts every developer must know: variables, control flow, functions, data structures, complexity.',
  topics: ['Variables & Types', 'Control Flow', 'Functions', 'Data Structures', 'Time Complexity'],
  estimatedMinutes: 20,
  questions: [
    {
      id: 'ff-001',
      topic: 'Time Complexity',
      difficulty: 'easy',
      prompt: 'What does Big O notation measure?',
      options: [
        { id: 'a', text: 'The exact number of operations' },
        { id: 'b', text: "The upper bound of an algorithm's growth rate relative to input size" },
        { id: 'c', text: 'The memory used by a program' },
        { id: 'd', text: 'The number of lines of code' },
      ],
      correctOptionId: 'b',
      definition: 'Big O notation describes the upper bound of an algorithm\'s time (or space) complexity as a function of input size n, focusing on the dominant growth factor.',
      explanation: 'Big O gives us a worst-case upper bound so we can compare algorithms independent of hardware or language. For example, O(n) means the work grows linearly with input size. It ignores constants and lower-order terms because we care about growth rate at scale.',
      whyOthersAreWrong: [
        'Option A: Big O does not count exact operations — it abstracts away constants and machine-specific details to express growth rate.',
        'Option C: Big O can describe space complexity, but it is not limited to memory; when stated alone it typically refers to time complexity in terms of growth rate.',
        'Option D: Lines of code is a static code metric unrelated to runtime behavior or algorithmic complexity.',
      ],
      example: 'A function that loops over an array of n items once is O(n). If you nest two loops over the same array, it becomes O(n²). Searching a sorted array with binary search is O(log n) — far better than scanning every element.',
    },
    {
      id: 'ff-002',
      topic: 'Data Structures',
      difficulty: 'easy',
      prompt: 'What is the time complexity of accessing an element in an array by index?',
      options: [
        { id: 'a', text: 'O(log n)' },
        { id: 'b', text: 'O(n)' },
        { id: 'c', text: 'O(1)' },
        { id: 'd', text: 'O(n²)' },
      ],
      correctOptionId: 'c',
      definition: 'Accessing an array element by index is O(1) — constant time — because arrays store elements in contiguous memory, so the address of any element can be computed directly from its index.',
      explanation: 'Given the base address and element size, the runtime calculates the address of index i as baseAddress + i * elementSize in a single operation. This does not depend on the size of the array at all, making it constant time regardless of whether the array has 10 or 10 million elements.',
      whyOthersAreWrong: [
        'Option A: O(log n) is characteristic of binary search or balanced tree lookups, not direct index access.',
        'Option B: O(n) would imply scanning each element to find the right one, which is what a linked list requires but not an array.',
        'Option D: O(n²) describes nested-loop algorithms and is not relevant to a simple memory address calculation.',
      ],
      example: 'In JavaScript, accessing `arr[42]` on an array of one million elements takes the same time as accessing `arr[0]`. In contrast, finding an element in a linked list at position 42 requires traversing 42 nodes — O(n).',
    },
    {
      id: 'ff-003',
      topic: 'Functions',
      difficulty: 'easy',
      prompt: 'What is a pure function?',
      options: [
        { id: 'a', text: 'A function that only uses global variables' },
        { id: 'b', text: 'A function that always returns the same output for the same input and has no side effects' },
        { id: 'c', text: 'A function with no parameters' },
        { id: 'd', text: 'A function that runs asynchronously' },
      ],
      correctOptionId: 'b',
      definition: 'A pure function is a function whose return value depends solely on its arguments and which produces no observable side effects such as modifying external state, writing to I/O, or mutating its inputs.',
      explanation: 'Pure functions are the foundation of functional programming. Because they are deterministic and side-effect-free, they are trivial to test (no mocks needed), safe to cache (memoize), and easy to reason about in isolation. Side effects like network calls or database writes should be isolated to the edges of your system.',
      whyOthersAreWrong: [
        'Option A: Reading global variables means the output can change if globals change, violating the determinism requirement of a pure function.',
        'Option C: Having no parameters would usually mean the function returns the same constant — while technically pure, this definition misses the core requirement of deterministic output based on inputs.',
        'Option D: Asynchronous execution is orthogonal to purity; an async function can be pure or impure.',
      ],
      example: 'In TypeScript: `const add = (a: number, b: number): number => a + b;` is pure. `const addAndLog = (a: number, b: number): number => { console.log(a); return a + b; }` is impure because `console.log` is a side effect.',
    },
    {
      id: 'ff-004',
      topic: 'Variables & Types',
      difficulty: 'easy',
      prompt: 'What does "pass by reference" mean?',
      options: [
        { id: 'a', text: 'A copy of the value is passed to the function' },
        { id: 'b', text: 'The function receives the memory address of the variable' },
        { id: 'c', text: 'The variable is converted to a string' },
        { id: 'd', text: 'A new variable is created inside the function' },
      ],
      correctOptionId: 'b',
      definition: 'Pass by reference means the function receives a reference (memory address) to the original variable, so modifications inside the function affect the original value in the caller\'s scope.',
      explanation: 'When a reference is passed, both the caller and the callee point to the same memory location. Changing the object inside the function changes it for everyone who holds that reference. In JavaScript, primitives are passed by value while objects and arrays are passed by reference (technically "pass by value of the reference").',
      whyOthersAreWrong: [
        'Option A: Passing a copy of the value describes "pass by value" — the function gets its own independent copy and changes do not affect the original.',
        'Option C: Type conversion is entirely unrelated to the pass-by-reference concept.',
        'Option D: Creating a new variable inside the function is what happens with pass by value, not pass by reference.',
      ],
      example: 'In JavaScript: `function mutate(obj) { obj.x = 99; } const o = { x: 1 }; mutate(o); console.log(o.x); // 99`. The object was mutated because its reference was passed. Contrast with primitives: `function inc(n) { n++; } let v = 1; inc(v); console.log(v); // still 1`.',
    },
    {
      id: 'ff-005',
      topic: 'Data Structures',
      difficulty: 'easy',
      prompt: 'What is the main difference between a stack and a queue?',
      options: [
        { id: 'a', text: 'A stack allows random access; a queue does not' },
        { id: 'b', text: 'A stack uses LIFO ordering; a queue uses FIFO ordering' },
        { id: 'c', text: 'A stack can only store integers; a queue can store any type' },
        { id: 'd', text: 'A stack is faster than a queue' },
      ],
      correctOptionId: 'b',
      definition: 'A stack is a Last-In, First-Out (LIFO) data structure where the last element added is the first removed. A queue is a First-In, First-Out (FIFO) structure where the first element added is the first removed.',
      explanation: 'Stacks model scenarios where you process the most recent item first — like browser back history, function call stacks, or undo operations. Queues model waiting lines where fairness requires processing in arrival order — like task schedulers or BFS traversal.',
      whyOthersAreWrong: [
        'Option A: Neither a stack nor a standard queue supports random index-based access — both are sequential structures.',
        'Option C: Both stacks and queues are generic data structures that can store any type; the type they store is unrelated to their ordering behavior.',
        'Option D: Asymptotic performance is equivalent for basic stack and queue operations; neither is inherently faster.',
      ],
      example: 'In JavaScript, a stack can be implemented with an array using `push` and `pop`. A queue uses `push` and `shift`. The call stack in your JavaScript engine is a stack — when `funcA` calls `funcB`, `funcB` is pushed; when it returns it is popped, and execution resumes in `funcA`.',
    },
    {
      id: 'ff-006',
      topic: 'Functions',
      difficulty: 'medium',
      prompt: 'What is recursion?',
      options: [
        { id: 'a', text: 'A loop that runs a fixed number of times' },
        { id: 'b', text: 'A function that calls itself to solve smaller subproblems' },
        { id: 'c', text: 'A function with multiple return statements' },
        { id: 'd', text: 'A method of sorting arrays' },
      ],
      correctOptionId: 'b',
      definition: 'Recursion is a technique where a function solves a problem by calling itself with a simpler or smaller version of the same input, continuing until it reaches a base case that can be solved directly.',
      explanation: 'Every recursive solution needs a base case (the stopping condition) and a recursive case (the self-call with reduced input). Without a base case the function recurses infinitely, causing a stack overflow. Recursion is natural for problems with hierarchical or self-similar structure like trees, graphs, and divide-and-conquer algorithms.',
      whyOthersAreWrong: [
        'Option A: A loop running a fixed number of times is a for loop — iteration, not recursion.',
        'Option C: Having multiple return statements is a common code structure feature unrelated to recursion.',
        'Option D: Sorting arrays is an application that can use recursion (e.g., merge sort), but recursion itself is a general technique not limited to sorting.',
      ],
      example: 'TypeScript factorial: `function factorial(n: number): number { if (n <= 1) return 1; return n * factorial(n - 1); }`. Calling `factorial(5)` calls `factorial(4)`, which calls `factorial(3)`, down to `factorial(1)` which returns 1, then unwinds.',
    },
    {
      id: 'ff-007',
      topic: 'Time Complexity',
      difficulty: 'medium',
      prompt: 'What does O(n log n) typically indicate?',
      options: [
        { id: 'a', text: 'A quadratic algorithm' },
        { id: 'b', text: 'A constant-time algorithm' },
        { id: 'c', text: 'An efficient sorting algorithm such as merge sort' },
        { id: 'd', text: 'An algorithm that only works on sorted data' },
      ],
      correctOptionId: 'c',
      definition: 'O(n log n) is the time complexity of comparison-based sorting algorithms like merge sort, heap sort, and the average case of quicksort — it represents the theoretical lower bound for sorting via comparisons.',
      explanation: 'O(n log n) grows faster than O(n) but far slower than O(n²). It arises naturally in divide-and-conquer algorithms: you divide the problem into halves (log n levels) and do O(n) work at each level. This is why merge sort is preferred over bubble sort (O(n²)) for large datasets.',
      whyOthersAreWrong: [
        'Option A: A quadratic algorithm is O(n²), which describes nested loops over the entire input — significantly worse than O(n log n).',
        'Option B: Constant time is O(1), meaning the algorithm takes the same time regardless of input size.',
        'Option D: O(n log n) algorithms like merge sort work on unsorted data; binary search requires sorted data but is O(log n) not O(n log n).',
      ],
      example: 'JavaScript\'s built-in `Array.prototype.sort()` uses an O(n log n) algorithm (Timsort in V8). Sorting 1,000,000 elements takes roughly 20,000,000 operations with O(n log n) versus 1,000,000,000,000 with O(n²) — a million times faster.',
    },
    {
      id: 'ff-008',
      topic: 'Data Structures',
      difficulty: 'medium',
      prompt: 'What is a hash map?',
      options: [
        { id: 'a', text: 'A sorted list of key-value pairs' },
        { id: 'b', text: 'A data structure that maps keys to values using a hash function for O(1) average lookup' },
        { id: 'c', text: 'A tree structure with hash-based ordering' },
        { id: 'd', text: 'An array that only stores hash codes' },
      ],
      correctOptionId: 'b',
      definition: 'A hash map (also called a hash table or dictionary) is a data structure that uses a hash function to map keys to bucket indices, enabling average O(1) insertion, deletion, and lookup.',
      explanation: 'The hash function converts any key into an integer index into an underlying array. Collisions (two keys hashing to the same index) are handled via chaining or open addressing. Because most operations are O(1) on average, hash maps are one of the most frequently used data structures in software engineering.',
      whyOthersAreWrong: [
        'Option A: A sorted list of key-value pairs describes a balanced BST-based map (e.g., C++ std::map), which provides O(log n) access, not O(1).',
        'Option C: Trees with hash-based ordering are not a standard structure; hash maps use arrays under the hood, not trees.',
        'Option D: Hash maps store the actual keys and values, not just hash codes; hash codes are only used internally to determine bucket positions.',
      ],
      example: 'In TypeScript: `const map = new Map<string, number>(); map.set("alice", 42); console.log(map.get("alice")); // 42`. Plain objects also behave as hash maps: `const scores: Record<string, number> = {}; scores["bob"] = 99;`.',
    },
    {
      id: 'ff-009',
      topic: 'Data Structures',
      difficulty: 'medium',
      prompt: 'What advantage does a linked list have over an array?',
      options: [
        { id: 'a', text: 'Faster index-based access' },
        { id: 'b', text: 'Less memory usage overall' },
        { id: 'c', text: 'O(1) insertion and deletion at known positions without shifting elements' },
        { id: 'd', text: 'Better cache performance' },
      ],
      correctOptionId: 'c',
      definition: 'A linked list stores elements as nodes where each node holds a value and a pointer to the next node. This structure allows O(1) insertion and deletion at a known node because only pointer updates are needed — no elements need to shift.',
      explanation: 'In an array, inserting at position i requires shifting all elements after i one spot to the right — O(n) work. A linked list avoids this by simply adjusting the previous node\'s pointer. However, this advantage only applies when you already have a reference to the relevant node; finding it still requires O(n) traversal.',
      whyOthersAreWrong: [
        'Option A: Index-based access in a linked list is O(n) because you must traverse from the head; arrays provide O(1) random access.',
        'Option B: Linked lists use more memory than arrays because each node stores an extra pointer in addition to the data.',
        'Option D: Arrays have better cache performance because elements are stored contiguously in memory; linked list nodes can be scattered across the heap.',
      ],
      example: 'Implementing an LRU cache often uses a doubly linked list combined with a hash map. The hash map gives O(1) lookup of a node, and the linked list gives O(1) removal and reinsertion at the head — the best of both structures.',
    },
    {
      id: 'ff-010',
      topic: 'Control Flow',
      difficulty: 'easy',
      prompt: 'When would you use binary search instead of linear search?',
      options: [
        { id: 'a', text: 'When the dataset is very small' },
        { id: 'b', text: 'When the array is sorted and you want O(log n) lookup' },
        { id: 'c', text: 'When elements can be duplicated' },
        { id: 'd', text: 'When the array is stored in a linked list' },
      ],
      correctOptionId: 'b',
      definition: 'Binary search is a search algorithm that repeatedly halves the search space by comparing the target to the middle element, achieving O(log n) time — but it requires the array to be sorted.',
      explanation: 'Each comparison eliminates half of the remaining candidates. For an array of one million elements, binary search needs at most 20 comparisons (log₂ 1,000,000 ≈ 20), whereas linear search needs up to one million. The prerequisite is that the data is sorted so that comparisons are meaningful for narrowing the search range.',
      whyOthersAreWrong: [
        'Option A: For very small datasets, the overhead of binary search is unnecessary — linear search is often just as fast and simpler.',
        'Option C: Duplicates do not prevent binary search; you simply need to handle finding the first or last occurrence, but sorting is still the core requirement.',
        'Option D: Binary search requires O(1) index access; linked lists provide O(n) access, making binary search on a linked list no better than linear search.',
      ],
      example: 'In C#: `Array.BinarySearch(sortedArray, target)` returns the index in O(log n). A phone book lookup is binary search in real life — you open in the middle, decide left or right, and repeat until found.',
    },
    {
      id: 'ff-011',
      topic: 'Control Flow',
      difficulty: 'easy',
      prompt: 'What is short-circuit evaluation in boolean expressions?',
      options: [
        { id: 'a', text: 'The program exits early when an error occurs' },
        { id: 'b', text: 'The second operand is not evaluated if the result is determined by the first operand' },
        { id: 'c', text: 'A compiler optimization that removes unused code' },
        { id: 'd', text: 'A technique for reducing nested if statements' },
      ],
      correctOptionId: 'b',
      definition: 'Short-circuit evaluation means that in a logical expression, the second operand is only evaluated if the first operand is insufficient to determine the overall result.',
      explanation: 'For `A && B`, if A is false the overall result is false regardless of B, so B is never evaluated. For `A || B`, if A is true the result is true regardless of B. This is both a performance optimization and a safety mechanism — you can guard against null dereference by checking `obj != null && obj.property`.',
      whyOthersAreWrong: [
        'Option A: Early program exit on error describes exception handling or early returns, not short-circuit evaluation.',
        'Option C: Dead code elimination is a compiler optimization phase; short-circuit evaluation is a language evaluation rule at runtime.',
        'Option D: Guard clauses reduce nesting, but they are a separate concept from short-circuit evaluation of boolean operators.',
      ],
      example: 'In TypeScript: `const name = user && user.profile && user.profile.name;` — if `user` is null, evaluation stops and `name` is null without throwing. Modern TypeScript uses optional chaining: `const name = user?.profile?.name;` — same idea, cleaner syntax.',
    },
    {
      id: 'ff-012',
      topic: 'Functions',
      difficulty: 'easy',
      prompt: 'What is the purpose of a default parameter in a function?',
      options: [
        { id: 'a', text: 'To make the function run faster' },
        { id: 'b', text: 'To provide a fallback value when an argument is not supplied by the caller' },
        { id: 'c', text: 'To prevent the function from being called without arguments' },
        { id: 'd', text: 'To define the return type of the function' },
      ],
      correctOptionId: 'b',
      definition: 'A default parameter specifies the value a parameter takes if the caller does not provide one (or passes `undefined`), making that argument optional without requiring the caller to handle it.',
      explanation: 'Default parameters improve API usability by making common cases require less code from the caller. They keep the function signature self-documenting about what the default behavior is. This avoids the old JavaScript pattern of `var x = arg !== undefined ? arg : defaultValue;` inside the function body.',
      whyOthersAreWrong: [
        'Option A: Default parameters have no effect on execution speed — they are evaluated lazily only when the argument is missing.',
        'Option C: Default parameters make arguments optional, which is the opposite of preventing calls without arguments.',
        'Option D: Return types are defined by type annotations (e.g., `: string`) or inferred by the compiler, not by default parameter values.',
      ],
      example: 'TypeScript: `function greet(name: string, greeting: string = "Hello"): string { return `${greeting}, ${name}!`; }`. Calling `greet("Alice")` returns `"Hello, Alice!"` while `greet("Alice", "Hi")` returns `"Hi, Alice!"`.',
    },
    {
      id: 'ff-013',
      topic: 'Variables & Types',
      difficulty: 'medium',
      prompt: 'What does "immutable" mean in programming?',
      options: [
        { id: 'a', text: 'A variable that cannot be reassigned after declaration' },
        { id: 'b', text: 'A value or object that cannot be changed after it is created' },
        { id: 'c', text: 'A function that has no return value' },
        { id: 'd', text: 'A type that only holds numeric values' },
      ],
      correctOptionId: 'b',
      definition: 'Immutability means that once a value or object is created, its content cannot be changed. Any "modification" produces a new object rather than altering the original.',
      explanation: 'Immutable data is inherently thread-safe (no concurrent modification issues), easier to reason about (no unexpected mutations), and enables powerful techniques like structural sharing and memoization. In JavaScript, strings and numbers are immutable primitives. Objects must be explicitly made immutable with `Object.freeze()` or immutable libraries.',
      whyOthersAreWrong: [
        'Option A: A variable that cannot be reassigned is described by `const` in JavaScript/TypeScript — but a `const` object\'s properties can still be mutated. That is variable binding, not object immutability.',
        'Option C: A function with no return value is a void function, completely unrelated to immutability.',
        'Option D: Numeric-only types are a type constraint; immutability is about mutability of values regardless of their type.',
      ],
      example: 'In TypeScript: `const arr = [1, 2, 3]; arr.push(4); // allowed — arr is mutable`. For immutability: `const frozen = Object.freeze([1, 2, 3]); frozen.push(4); // throws in strict mode`. Libraries like Immer let you write mutating-style code that produces immutable updates.',
    },
    {
      id: 'ff-014',
      topic: 'Variables & Types',
      difficulty: 'easy',
      prompt: 'What is the difference between == and === in JavaScript?',
      options: [
        { id: 'a', text: '== checks value only; === checks value and type' },
        { id: 'b', text: '== is faster than ===' },
        { id: 'c', text: '== works only on numbers; === works on all types' },
        { id: 'd', text: 'There is no difference in modern JavaScript' },
      ],
      correctOptionId: 'a',
      definition: '`==` is the loose equality operator that performs type coercion before comparing; `===` is the strict equality operator that requires both value and type to be identical.',
      explanation: '`==` can produce surprising results: `0 == false` is `true`, `"" == false` is `true`, and `null == undefined` is `true`. `===` never coerces types, so `0 === false` is `false`. Modern JavaScript best practice is to always use `===` to avoid subtle bugs from implicit coercion.',
      whyOthersAreWrong: [
        'Option B: The performance difference between `==` and `===` is negligible and not a meaningful reason to choose one over the other.',
        'Option C: Both operators work on all JavaScript types including strings, booleans, objects, and numbers.',
        'Option D: The difference is real and significant — `==` still performs type coercion in all modern JavaScript engines.',
      ],
      example: '`1 == "1"` is `true` because `"1"` is coerced to `1`. `1 === "1"` is `false` because a number and a string are different types. In TypeScript, using `===` is standard and the linter will often warn against `==`.',
    },
    {
      id: 'ff-015',
      topic: 'Functions',
      difficulty: 'hard',
      prompt: 'What is tail recursion?',
      options: [
        { id: 'a', text: 'Recursion that only processes the last element of a list' },
        { id: 'b', text: 'A recursive call that is the last operation in the function, allowing the compiler to optimize stack usage' },
        { id: 'c', text: 'A form of recursion that uses two base cases' },
        { id: 'd', text: 'Recursion with a depth limit' },
      ],
      correctOptionId: 'b',
      definition: 'Tail recursion occurs when the recursive call is the very last operation in a function — there is nothing left to do after the call returns. Compilers that support TCO (Tail Call Optimization) can reuse the current stack frame instead of creating a new one.',
      explanation: 'In normal recursion, each call adds a frame to the call stack, risking stack overflow for deep recursion. In tail-recursive functions, because nothing needs to happen after the recursive call, the compiler can replace the current frame with the new one, making recursion as memory-efficient as iteration. Note: JavaScript engines are not required to implement TCO (and most do not), but the concept is important in functional languages like Haskell, Elixir, and Scala.',
      whyOthersAreWrong: [
        'Option A: Processing the last element of a list is a specific use case; tail recursion is defined by the call position in the function, not the data being processed.',
        'Option C: The number of base cases is unrelated to whether a recursive call is in tail position.',
        'Option D: Depth-limited recursion is a separate technique to prevent stack overflow; it does not describe what tail recursion means structurally.',
      ],
      example: 'Normal recursion: `factorial(n) = n * factorial(n-1)` — multiplication happens after the recursive call (not tail). Tail-recursive: `factTail(n, acc = 1) = n <= 1 ? acc : factTail(n-1, n * acc)` — the recursive call is the last thing, with no pending multiplication.',
    },
  ],
};
