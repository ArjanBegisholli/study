import type { Test } from '../../types/quiz';

export const midAlgorithms: Test = {
  id: 'mid-algorithms',
  role: 'mid',
  title: 'Data Structures, Algorithms & Performance',
  description: 'Deeper knowledge of data structures, algorithm analysis, and performance optimization techniques.',
  topics: ['Hash Maps', 'Trees', 'Heaps', 'Graphs', 'Sorting', 'Complexity'],
  estimatedMinutes: 25,
  questions: [
    {
      id: 'ma-001',
      topic: 'Hash Maps',
      difficulty: 'easy',
      prompt: 'What is the average time complexity for a hash map lookup?',
      options: [
        { id: 'a', text: 'O(n)' },
        { id: 'b', text: 'O(log n)' },
        { id: 'c', text: 'O(1)' },
        { id: 'd', text: 'O(n²)' },
      ],
      correctOptionId: 'c',
      definition: 'A hash map lookup is O(1) on average because the hash function computes the bucket index directly, making the lookup a constant-time calculation regardless of map size.',
      explanation: 'The hash function converts a key to an index in O(1), then a direct memory access retrieves the value. Collisions can theoretically cause O(n) worst-case (all keys hash to one bucket), but with a good hash function and load factor management, the average stays O(1). This is why hash maps are the go-to for fast key-value access.',
      whyOthersAreWrong: [
        'Option A: O(n) describes linear scan of a list or array without an index; hash maps avoid this by using a hash function for direct bucket access.',
        'Option B: O(log n) describes binary search tree lookups; hash maps do not maintain sorted order and achieve faster average access.',
        'Option D: O(n²) describes nested loops; this is never the case for a hash map lookup under any normal conditions.',
      ],
      example: 'JavaScript `Map.get()` and plain object property access are O(1) average. Counting word frequencies across a million-word document: `words.forEach(w => freq.set(w, (freq.get(w) ?? 0) + 1))` — each `get` and `set` is O(1), making the whole pass O(n).',
    },
    {
      id: 'ma-002',
      topic: 'Trees',
      difficulty: 'medium',
      prompt: 'What property defines a Binary Search Tree (BST)?',
      options: [
        { id: 'a', text: 'Each node has exactly two children' },
        { id: 'b', text: 'For each node, all left subtree values are less and all right subtree values are greater' },
        { id: 'c', text: 'The tree is always balanced' },
        { id: 'd', text: 'The root always contains the minimum value' },
      ],
      correctOptionId: 'b',
      definition: 'A Binary Search Tree (BST) is a binary tree where every node satisfies the BST property: all values in its left subtree are strictly less, and all values in its right subtree are strictly greater.',
      explanation: 'This ordering property makes BSTs useful for efficient search, insertion, and deletion — all O(log n) in a balanced tree, because at each node you eliminate half the remaining search space by going left or right. In-order traversal of a BST yields a sorted sequence.',
      whyOthersAreWrong: [
        'Option A: Having exactly two children is not required — BST nodes can have zero, one, or two children. The ordering property is what defines a BST.',
        'Option C: BSTs are not always balanced. An unbalanced BST (e.g., insertions in sorted order) degrades to O(n) operations. Self-balancing trees like AVL or Red-Black Trees add balance guarantees.',
        'Option D: The minimum value is always in the leftmost node of the tree, not the root; the root contains whatever value was inserted first (or the result of rotations in a balanced BST).',
      ],
      example: 'Inserting [5, 3, 7, 1, 4] into a BST: 5 is root, 3 goes left (less than 5), 7 goes right (greater than 5), 1 goes left of 3, 4 goes right of 3. Searching for 4: go left at 5, go right at 3, found at 4 — only 3 comparisons.',
    },
    {
      id: 'ma-003',
      topic: 'Heaps',
      difficulty: 'medium',
      prompt: 'When would you use a heap data structure?',
      options: [
        { id: 'a', text: 'For fast sorted iteration over a dataset' },
        { id: 'b', text: 'To implement a priority queue where the highest (or lowest) priority element is always accessible in O(1)' },
        { id: 'c', text: 'For storing key-value pairs' },
        { id: 'd', text: 'For representing graph edges' },
      ],
      correctOptionId: 'b',
      definition: 'A heap is a complete binary tree satisfying the heap property (max-heap: parent ≥ children; min-heap: parent ≤ children), making it ideal for implementing a priority queue with O(1) peek and O(log n) insert/remove.',
      explanation: 'Heaps are the best data structure when you repeatedly need the maximum or minimum element from a dynamic set. They support O(1) peek at the extreme element, O(log n) insertion, and O(log n) extraction. Heaps are used in Dijkstra\'s shortest path algorithm, heap sort, and scheduling systems.',
      whyOthersAreWrong: [
        'Option A: Sorted iteration requires extracting all elements in order — O(n log n) total. If you need fully sorted iteration, an array sort is cleaner; heaps are optimal when you only need the next maximum/minimum repeatedly.',
        'Option C: Key-value storage is the domain of hash maps or balanced BSTs; heaps prioritize by value and do not support arbitrary key-based lookup.',
        'Option D: Graph edges are stored as adjacency lists or adjacency matrices; heaps are used within graph algorithms (like Dijkstra) but are not used to represent the graph itself.',
      ],
      example: 'A job scheduler processes tasks by priority: `const pq = new MinHeap(); pq.insert({ priority: 3, task: "send email" }); pq.insert({ priority: 1, task: "process payment" }); pq.extractMin(); // { priority: 1, task: "process payment" }` — highest-priority task always first.',
    },
    {
      id: 'ma-004',
      topic: 'Graphs',
      difficulty: 'medium',
      prompt: 'What is the difference between BFS and DFS graph traversal?',
      options: [
        { id: 'a', text: 'BFS uses a stack; DFS uses a queue' },
        { id: 'b', text: 'BFS explores neighbors level-by-level using a queue; DFS explores as deep as possible using a stack or recursion' },
        { id: 'c', text: 'BFS is only for directed graphs; DFS works on both' },
        { id: 'd', text: 'There is no difference in terms of nodes visited' },
      ],
      correctOptionId: 'b',
      definition: 'BFS (Breadth-First Search) explores all nodes at the current depth before going deeper, using a queue. DFS (Depth-First Search) explores as far down a branch as possible before backtracking, using a stack or recursion.',
      explanation: 'BFS finds the shortest path in an unweighted graph because it explores by increasing distance. DFS uses less memory in sparse wide graphs and is better for topological sorting, cycle detection, and maze solving. Both visit all reachable nodes — the difference is the order of visitation.',
      whyOthersAreWrong: [
        'Option A: This is reversed — BFS uses a queue (FIFO) to process neighbors in order; DFS uses a stack (LIFO) or recursion to go deep first.',
        'Option C: Both BFS and DFS work on directed and undirected graphs; the graph type does not restrict which traversal can be used.',
        'Option D: Both visit the same set of reachable nodes, but the order and intermediate structure differ significantly, leading to different use cases.',
      ],
      example: 'Finding shortest route between two cities in a road map: BFS explores all roads 1 hop away, then 2 hops, guaranteeing the shortest path. Solving a maze where you want to find any path: DFS goes deep into each corridor before backtracking, typically using less memory.',
    },
    {
      id: 'ma-005',
      topic: 'Sorting',
      difficulty: 'medium',
      prompt: 'What is the average time complexity of quicksort?',
      options: [
        { id: 'a', text: 'O(n²)' },
        { id: 'b', text: 'O(n)' },
        { id: 'c', text: 'O(n log n)' },
        { id: 'd', text: 'O(log n)' },
      ],
      correctOptionId: 'c',
      definition: 'Quicksort has an average time complexity of O(n log n) due to its divide-and-conquer strategy: each partition step is O(n) and there are O(log n) levels of recursion on average.',
      explanation: 'Quicksort picks a pivot, partitions elements into less-than and greater-than groups, then recursively sorts each group. On average, the pivot splits the array roughly in half at each level — log n levels of O(n) work = O(n log n). Worst case is O(n²) with a consistently bad pivot (e.g., always picking the minimum on a sorted array), but randomized pivot selection makes this extremely unlikely.',
      whyOthersAreWrong: [
        'Option A: O(n²) is the worst case for quicksort with a bad pivot strategy (e.g., sorted input with first-element pivot). Average case is O(n log n).',
        'Option B: O(n) is only achievable for specialized sorts like counting sort or radix sort that exploit the data structure; comparison-based sorts have a theoretical lower bound of O(n log n).',
        'Option D: O(log n) describes operations on balanced trees or binary search — sorting requires visiting every element and is bounded below by O(n).',
      ],
      example: 'JavaScript\'s V8 engine uses Timsort (a hybrid of merge sort and insertion sort) for `Array.sort()`. For interview problems, recognizing that sorting an array costs O(n log n) helps analyze total algorithm complexity — e.g., sorting + linear scan = O(n log n) overall.',
    },
    {
      id: 'ma-006',
      topic: 'Sorting',
      difficulty: 'medium',
      prompt: 'What makes merge sort a stable sort?',
      options: [
        { id: 'a', text: 'It always runs in O(n log n)' },
        { id: 'b', text: 'It preserves the relative order of equal elements from the original input' },
        { id: 'c', text: 'It sorts in place without extra memory' },
        { id: 'd', text: 'It works correctly on linked lists' },
      ],
      correctOptionId: 'b',
      definition: 'A sorting algorithm is stable if it preserves the relative order of elements that compare as equal — if two records have the same key, they appear in the same order in the output as they did in the input.',
      explanation: 'Merge sort is stable because during the merge step, when two elements are equal, it always takes from the left (earlier) array first, preserving their original order. Stability matters when sorting objects by one key after previously sorting by another — e.g., sorting by last name, then by department, while keeping last-name order within each department.',
      whyOthersAreWrong: [
        'Option A: Running in O(n log n) describes merge sort\'s time complexity guarantee; stability is a separate property about preserving equal-element ordering.',
        'Option C: Merge sort requires O(n) extra memory for the merge step — it is not an in-place sort. In-place sorts like heapsort are not stable.',
        'Option D: Working on linked lists is an implementation flexibility of merge sort, but it is unrelated to the concept of stability.',
      ],
      example: 'Sorting employees first by department, then by salary within each department: `employees.sort((a, b) => a.salary - b.salary)` then `employees.sort((a, b) => a.dept.localeCompare(b.dept))`. If the second sort is stable, the within-department salary order from the first sort is preserved.',
    },
    {
      id: 'ma-007',
      topic: 'Hash Maps',
      difficulty: 'easy',
      prompt: 'When would you use a Set over an array?',
      options: [
        { id: 'a', text: 'When you need ordered, indexed access' },
        { id: 'b', text: 'When you need to track unique elements and check membership in O(1) average time' },
        { id: 'c', text: 'When you need to store key-value pairs' },
        { id: 'd', text: 'When the data must be sorted' },
      ],
      correctOptionId: 'b',
      definition: 'A Set is a collection of unique values that provides O(1) average time for `add`, `has`, and `delete` operations using hashing internally, making it ideal for membership testing and deduplication.',
      explanation: 'Checking if an element exists in an array requires O(n) scanning. A Set\'s `has()` is O(1) average. Sets also automatically deduplicate — adding the same value twice results in one entry. When your primary operations are "does this element exist?" or "keep only unique elements", a Set outperforms an array.',
      whyOthersAreWrong: [
        'Option A: Ordered indexed access (e.g., `arr[5]`) is an array feature; Sets are unordered and do not support index-based access.',
        'Option C: Key-value pairs are the domain of Maps (hash maps); Sets store only values, not key-value associations.',
        'Option D: Sets do not maintain sorted order; if you need sorted unique data, use a sorted array or a sorted set from a library.',
      ],
      example: 'Finding duplicate IDs in an array: `const seen = new Set<number>(); const duplicates = ids.filter(id => seen.has(id) ? true : (seen.add(id), false));`. Each `has` and `add` is O(1), making the whole operation O(n) instead of O(n²) with nested array scans.',
    },
    {
      id: 'ma-008',
      topic: 'Hash Maps',
      difficulty: 'medium',
      prompt: 'How does a hash map handle collisions?',
      options: [
        { id: 'a', text: 'It resizes the array immediately' },
        { id: 'b', text: 'Using techniques like chaining (linked lists per bucket) or open addressing (probing for the next empty slot)' },
        { id: 'c', text: 'It rejects the new key' },
        { id: 'd', text: 'It sorts the keys before inserting' },
      ],
      correctOptionId: 'b',
      definition: 'A collision occurs when two different keys produce the same hash bucket index. Hash maps handle this with chaining (each bucket holds a linked list of entries) or open addressing (probe for the next available slot).',
      explanation: 'In chaining, each bucket is a list; all colliding entries are appended to that list. Lookup scans the list linearly — O(1) average if chains are short. In open addressing (linear probing, quadratic probing, double hashing), a collision causes the algorithm to search for the next empty slot in the array. Modern implementations like Python dicts use open addressing with Robin Hood hashing.',
      whyOthersAreWrong: [
        'Option A: Resizing (rehashing) happens when the load factor exceeds a threshold (e.g., 0.75), not immediately on every collision.',
        'Option C: Rejecting a new key on collision would break the hash map\'s fundamental contract of storing all inserted key-value pairs.',
        'Option D: Sorting keys would not resolve collisions; the collision is about bucket indices produced by the hash function, not key ordering.',
      ],
      example: 'With chaining: bucket 3 has entries `[("cat", 1), ("act", 2)]` because both "cat" and "act" hash to index 3. Lookup for "act" goes to bucket 3, then scans the list for key equality. With a good hash function, most chains have length 1.',
    },
    {
      id: 'ma-009',
      topic: 'Graphs',
      difficulty: 'hard',
      prompt: 'How can you detect a cycle in a directed graph?',
      options: [
        { id: 'a', text: 'By checking if any node has more than two edges' },
        { id: 'b', text: 'Using DFS and tracking nodes currently in the recursion stack (gray nodes)' },
        { id: 'c', text: 'By checking if the graph has more edges than nodes' },
        { id: 'd', text: 'Using BFS and looking for a node visited twice' },
      ],
      correctOptionId: 'b',
      definition: 'Cycle detection in a directed graph is performed with DFS using three states: unvisited (white), in current DFS path (gray), and fully processed (black). A cycle exists if DFS encounters a gray node.',
      explanation: 'A gray node means we are currently in a recursive DFS path that includes that node — encountering it again means we have a back edge, which defines a cycle in a directed graph. Simply tracking "visited" (black) nodes would give false positives in DAGs (Directed Acyclic Graphs) where a node is reachable via multiple paths without a cycle.',
      whyOthersAreWrong: [
        'Option A: The number of outgoing edges from a node has no bearing on whether the graph contains a cycle; a node with many edges might be in a DAG.',
        'Option C: In a directed graph, more edges than nodes does not guarantee a cycle — this heuristic (Euler\'s theorem) applies to undirected graphs; even there it is only a necessary, not sufficient, condition.',
        'Option D: BFS with a visited set can detect cycles in undirected graphs, but in directed graphs, visiting a node twice does not indicate a cycle — the direction of edges matters.',
      ],
      example: 'Detecting circular imports in a module system: module A imports B, B imports C, C imports A — DFS on the dependency graph would encounter node A in the gray state when processing C\'s dependencies, flagging the cycle before the runtime crashes.',
    },
    {
      id: 'ma-010',
      topic: 'Complexity',
      difficulty: 'medium',
      prompt: 'What is memoization?',
      options: [
        { id: 'a', text: 'Storing results of expensive function calls and returning the cached result when the same inputs occur again' },
        { id: 'b', text: 'Compiling code ahead-of-time for faster execution' },
        { id: 'c', text: 'Storing all intermediate values in a global array' },
        { id: 'd', text: 'A way to reduce memory usage by recomputing values' },
      ],
      correctOptionId: 'a',
      definition: 'Memoization is an optimization technique that caches the return values of a function based on its inputs, so subsequent calls with the same arguments return the cached result without re-executing the function.',
      explanation: 'Memoization converts a function from computing the same result repeatedly to a table lookup after the first call. It transforms the time complexity of overlapping-subproblem algorithms from exponential to polynomial. It is the top-down counterpart of bottom-up dynamic programming, and is applicable to pure functions where the same inputs always produce the same output.',
      whyOthersAreWrong: [
        'Option B: Ahead-of-time compilation is a build-time optimization by the compiler; memoization is a runtime caching technique implemented in code.',
        'Option C: Memoization stores specifically the results of function calls indexed by their inputs — not all intermediate values globally. The storage is selective and input-keyed.',
        'Option D: Memoization uses more memory (to store cached results) to reduce computation time — it trades memory for speed, not the other way around.',
      ],
      example: 'Fibonacci without memoization: O(2ⁿ). With memoization: `const memo = new Map(); function fib(n: number): number { if (n <= 1) return n; if (memo.has(n)) return memo.get(n)!; const result = fib(n-1) + fib(n-2); memo.set(n, result); return result; }` — O(n) time.',
    },
    {
      id: 'ma-011',
      topic: 'Complexity',
      difficulty: 'hard',
      prompt: 'What is dynamic programming?',
      options: [
        { id: 'a', text: 'A programming paradigm for concurrent code' },
        { id: 'b', text: 'Breaking a problem into overlapping subproblems, solving each once, and storing results to avoid redundant work' },
        { id: 'c', text: 'A runtime optimization applied by the compiler' },
        { id: 'd', text: 'An algorithm design approach only for graph problems' },
      ],
      correctOptionId: 'b',
      definition: 'Dynamic programming (DP) is an algorithm design technique that solves complex problems by breaking them into overlapping subproblems, solving each subproblem once, and storing the results in a table to avoid redundant computation.',
      explanation: 'DP applies when a problem has two properties: optimal substructure (the optimal solution to the problem contains optimal solutions to its subproblems) and overlapping subproblems (the same subproblems are solved multiple times in naive recursion). DP can be implemented top-down with memoization or bottom-up with tabulation.',
      whyOthersAreWrong: [
        'Option A: Concurrent programming paradigms deal with threads and synchronization; DP has nothing to do with concurrency.',
        'Option C: Compiler runtime optimizations are applied transparently by the toolchain; DP is an explicit algorithm design choice made by the programmer.',
        'Option D: While DP solves many graph problems (shortest paths, DAG optimization), it also applies to string problems (edit distance, LCS), sequence problems (LIS), knapsack, and many other domains.',
      ],
      example: 'Longest Common Subsequence: naive recursion is O(2ⁿ). DP tabulation: fill an (m×n) table bottom-up where `dp[i][j]` = LCS length of first i characters of s1 and first j characters of s2. O(m×n) time and space. Classic in diff tools and bioinformatics.',
    },
    {
      id: 'ma-012',
      topic: 'Complexity',
      difficulty: 'medium',
      prompt: "What does \"amortized O(1)\" mean for a dynamic array's append operation?",
      options: [
        { id: 'a', text: 'Each individual append is O(1)' },
        { id: 'b', text: 'The average cost per operation over many operations is O(1), even though occasional resizes cost O(n)' },
        { id: 'c', text: 'Append is always faster than O(1)' },
        { id: 'd', text: 'It means the worst case is O(log n)' },
      ],
      correctOptionId: 'b',
      definition: 'Amortized analysis spreads the cost of expensive occasional operations over many cheap operations. For a dynamic array, most appends are O(1) but periodic resizing is O(n); the amortized cost per append is O(1).',
      explanation: 'When a dynamic array doubles in size, it copies n elements — O(n). But this doubling means the next n appends will be O(1) each. Total cost for n appends: O(n) resizes + O(n) cheap appends = O(n), so cost per append = O(n)/n = O(1) amortized. JavaScript\'s arrays, Python\'s lists, and .NET\'s `List<T>` all use this strategy.',
      whyOthersAreWrong: [
        'Option A: Individual appends are O(1) most of the time, but not always — when the array needs to resize, that append costs O(n). Amortized means averaged over many appends.',
        'Option C: No operation can be faster than O(1); O(1) is already the minimum constant time.',
        'Option D: Amortized O(1) means the average is constant, not that the worst case is O(log n).',
      ],
      example: 'A dynamic array starts with capacity 1. Appending 16 elements triggers resizes at sizes 1→2→4→8→16, costing 1+2+4+8+16 = 31 copy operations. Spread over 16 appends, that is ~2 operations per append on average — constant amortized cost.',
    },
    {
      id: 'ma-013',
      topic: 'Trees',
      difficulty: 'hard',
      prompt: 'What problem does an LRU (Least Recently Used) cache solve?',
      options: [
        { id: 'a', text: 'Sorting data in a tree structure' },
        { id: 'b', text: 'Evicting the least recently accessed item when the cache is full to keep frequently accessed items available' },
        { id: 'c', text: 'Preventing hash collisions' },
        { id: 'd', text: 'Storing graph edges efficiently' },
      ],
      correctOptionId: 'b',
      definition: 'An LRU (Least Recently Used) cache is a fixed-capacity data structure that automatically evicts the least recently accessed entry when the cache is full, based on the assumption that recently used items are more likely to be needed again.',
      explanation: 'LRU caches are commonly used to cache database query results, API responses, or computed values with limited memory. The optimal implementation uses a hash map for O(1) key lookup combined with a doubly linked list for O(1) promotion of recently accessed entries and O(1) removal of the least recently used entry.',
      whyOthersAreWrong: [
        'Option A: Sorting data in trees is the domain of BSTs and B-trees; an LRU cache is about eviction policy, not sorting.',
        'Option C: Hash collision prevention is an internal detail of hash map implementation; LRU caches use hash maps but their purpose is eviction, not collision handling.',
        'Option D: Graph edge storage uses adjacency lists or adjacency matrices; an LRU cache is a general-purpose bounded cache, not a graph structure.',
      ],
      example: 'Node.js module caching uses LRU. In a web server: cache up to 100 database query results. When the 101st unique query comes in, evict the result for whichever of the 100 cached queries was accessed least recently. Frequently-hit queries stay warm.',
    },
    {
      id: 'ma-014',
      topic: 'Complexity',
      difficulty: 'medium',
      prompt: 'What is the space-time tradeoff?',
      options: [
        { id: 'a', text: 'Using more CPU cores to save memory' },
        { id: 'b', text: 'Using additional memory (space) to store precomputed values in order to reduce computation time' },
        { id: 'c', text: 'Choosing between a fast language and a memory-safe language' },
        { id: 'd', text: 'Reducing code size to improve runtime speed' },
      ],
      correctOptionId: 'b',
      definition: 'The space-time tradeoff is the principle that an algorithm can be made faster by using more memory to cache or precompute values, or use less memory by recomputing values on demand — trading one resource for the other.',
      explanation: 'Hash maps, lookup tables, memoization, and precomputed indices all trade memory for speed. Conversely, streaming algorithms process data in passes without storing it all at once, trading time (multiple passes) for space (constant memory). Understanding this tradeoff is fundamental to algorithm design for resource-constrained environments.',
      whyOthersAreWrong: [
        'Option A: Using more CPU cores to parallelize work is a concurrency strategy, not the space-time tradeoff.',
        'Option C: Language choice is a technology decision involving many factors; the space-time tradeoff is a fundamental algorithmic concept.',
        'Option D: Code size (binary size) is mostly irrelevant to the space-time tradeoff, which is about runtime memory usage versus computation time.',
      ],
      example: 'Precomputing a lookup table for trigonometric values in a graphics engine trades memory (storing a table of 360 values) for speed (table lookup instead of computing sin/cos each frame). A chess engine stores transposition tables (previously evaluated positions) to avoid re-evaluating the same board state.',
    },
    {
      id: 'ma-015',
      topic: 'Hash Maps',
      difficulty: 'hard',
      prompt: 'What is a Bloom filter?',
      options: [
        { id: 'a', text: 'A balanced tree that filters duplicate keys' },
        { id: 'b', text: 'A probabilistic data structure that efficiently tests whether an element is possibly in a set, with no false negatives' },
        { id: 'c', text: 'A hash function that prevents collisions' },
        { id: 'd', text: 'A compression algorithm for sorted arrays' },
      ],
      correctOptionId: 'b',
      definition: 'A Bloom filter is a space-efficient probabilistic data structure that uses multiple hash functions and a bit array to test set membership, returning either "definitely not in set" or "possibly in set" — it has no false negatives but allows false positives.',
      explanation: 'A Bloom filter uses a bit array and k hash functions. To add an element, set the bits at all k hash positions. To test membership, check all k bits — if any is 0, the element is definitely absent; if all are 1, the element is probably present (but could be a false positive from other elements setting those bits). This gives sub-linear space usage at the cost of small false-positive probability.',
      whyOthersAreWrong: [
        'Option A: Balanced trees (AVL, Red-Black) are deterministic exact data structures; Bloom filters are probabilistic and array-based.',
        'Option C: Collision prevention is a hash function design goal; a Bloom filter deliberately uses multiple hash functions and accepts probabilistic membership testing.',
        'Option D: Compression algorithms reduce data size through encoding; Bloom filters are membership-test structures, not compression algorithms.',
      ],
      example: 'Google Chrome\'s Safe Browsing uses a Bloom filter to check URLs against a list of malicious sites. If the filter says "not malicious", it definitely is safe (no false negatives). If it says "possibly malicious", Chrome does a full database lookup to confirm (false positive → extra network call, but fast negative is the common case).',
    },
  ],
};
