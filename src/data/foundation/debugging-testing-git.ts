import type { Test } from '../../types/quiz';

export const foundationDebuggingTestingGit: Test = {
  id: 'foundation-debugging-testing-git',
  role: 'foundation',
  title: 'Debugging, Testing & Git',
  description: 'Core skills for verifying correctness, finding bugs, and managing code history.',
  topics: ['Debugging', 'Unit Tests', 'Integration Tests', 'Git Basics'],
  estimatedMinutes: 20,
  questions: [
    {
      id: 'fdtg-001',
      topic: 'Debugging',
      difficulty: 'easy',
      prompt: 'What is a stack trace?',
      options: [
        { id: 'a', text: 'A list of all variables in memory' },
        { id: 'b', text: 'A report showing the sequence of function calls that led to an error' },
        { id: 'c', text: 'A visual representation of the call graph' },
        { id: 'd', text: 'A log of all HTTP requests made by the app' },
      ],
      correctOptionId: 'b',
      definition: 'A stack trace (or stack traceback) is a report of the active stack frames at a point in time — typically at the moment an exception is thrown — showing the sequence of function calls that led to the error.',
      explanation: 'Reading a stack trace from top to bottom shows you where the error occurred and exactly how execution arrived there. The top frame is usually the direct site of the error; frames below it show the chain of callers. Stack traces are the primary first clue when diagnosing an unexpected runtime failure.',
      whyOthersAreWrong: [
        'Option A: A list of variables in memory describes a memory dump or heap snapshot, not a stack trace.',
        'Option C: A visual call graph is a diagram produced by profiling tools; a stack trace is a text report of the call chain at a specific moment.',
        'Option D: HTTP request logs are produced by network monitoring or application logging middleware; they capture I/O activity, not function call chains.',
      ],
      example: 'In Node.js: `TypeError: Cannot read properties of undefined (reading "name") at getUser (user.js:12:20) at handleRequest (server.js:34:5) at Layer.handle ...` — line `user.js:12` is where to look first, and `server.js:34` shows what called it.',
    },
    {
      id: 'fdtg-002',
      topic: 'Unit Tests',
      difficulty: 'easy',
      prompt: 'What is a unit test?',
      options: [
        { id: 'a', text: 'A test that verifies an entire application end-to-end' },
        { id: 'b', text: 'An automated test that verifies a small, isolated piece of code (a function or class) in isolation' },
        { id: 'c', text: 'A manual test performed by a QA engineer' },
        { id: 'd', text: 'A performance benchmark for a single function' },
      ],
      correctOptionId: 'b',
      definition: 'A unit test is an automated test that verifies a single unit of code (typically a function or class method) in isolation from its dependencies, which are replaced with mocks or stubs.',
      explanation: 'Unit tests are the most granular and fastest type of automated test. Because they test in isolation, a failing unit test points directly to the broken unit. They provide a safety net for refactoring, serve as living documentation, and should be the most numerous layer of the test pyramid.',
      whyOthersAreWrong: [
        'Option A: Testing the entire application end-to-end describes an end-to-end (E2E) test, which is at the opposite end of the test pyramid from unit tests.',
        'Option C: Manual tests are not unit tests; unit tests are automated by definition and run without human interaction.',
        'Option D: Performance benchmarks measure speed and throughput; unit tests verify functional correctness.',
      ],
      example: 'In Jest/TypeScript: `test("add returns the sum", () => { expect(add(2, 3)).toBe(5); });`. This tests only the `add` function in isolation — no network, no database, runs in milliseconds.',
    },
    {
      id: 'fdtg-003',
      topic: 'Unit Tests',
      difficulty: 'easy',
      prompt: 'What does the test pyramid describe?',
      options: [
        { id: 'a', text: 'The hierarchy of testing frameworks available' },
        { id: 'b', text: 'The recommended ratio of unit, integration, and end-to-end tests — many unit tests, fewer integration, fewest E2E' },
        { id: 'c', text: 'A method for organizing test files in a project' },
        { id: 'd', text: 'The order in which tests should be run' },
      ],
      correctOptionId: 'b',
      definition: 'The test pyramid is a guideline introduced by Mike Cohn that recommends a large base of unit tests, a middle layer of integration tests, and a small top of end-to-end tests, reflecting cost-effectiveness and speed.',
      explanation: 'Unit tests are cheap to write, fast to run, and pinpoint failures precisely. Integration and E2E tests are slower, more brittle, and harder to diagnose. Having too many E2E tests (an "ice cream cone" antipattern) makes CI slow and flaky. The pyramid guides where to invest testing effort for the best return.',
      whyOthersAreWrong: [
        'Option A: The test pyramid says nothing about frameworks — it is about the ratio and types of tests.',
        'Option C: File organization is a project structure concern unrelated to the test pyramid.',
        'Option D: Test execution order is a runner configuration concern; the pyramid is about the proportion of different test types, not their order.',
      ],
      example: 'A well-structured project might have 500 unit tests (run in seconds), 50 integration tests (run in a minute), and 10 E2E tests (run in 5 minutes). This pyramid shape keeps CI fast while maintaining thorough coverage.',
    },
    {
      id: 'fdtg-004',
      topic: 'Unit Tests',
      difficulty: 'medium',
      prompt: 'What is mocking in tests?',
      options: [
        { id: 'a', text: 'Writing fake data to a database' },
        { id: 'b', text: 'Replacing real dependencies with controlled fake implementations to isolate the unit under test' },
        { id: 'c', text: 'Running tests in a simulated browser' },
        { id: 'd', text: 'Hiding implementation details from test files' },
      ],
      correctOptionId: 'b',
      definition: 'Mocking is a test technique where real dependencies (databases, external APIs, file systems) are replaced with controlled fake objects (mocks) that simulate the dependency\'s behavior without actually executing it.',
      explanation: 'Mocks let you test a unit in isolation by controlling exactly what its dependencies return, including error conditions that would be hard to trigger with real systems. This makes tests fast, deterministic, and not reliant on external services being available. Frameworks like Jest, Moq (.NET), and Mockito (Java) provide mocking infrastructure.',
      whyOthersAreWrong: [
        'Option A: Writing fake data to a database describes a test fixture or seed script; mocking replaces the database entirely so no real database is involved.',
        'Option C: Running tests in a simulated browser describes using tools like JSDOM or Playwright; mocking is about replacing dependencies at the code level.',
        'Option D: Hiding implementation details describes encapsulation or information hiding in OOP; mocking is specifically about substituting dependencies in tests.',
      ],
      example: 'Testing `UserService.createUser(email)` which calls `emailService.sendWelcomeEmail(email)`: you mock `emailService` to return a resolved promise without actually sending an email. Your test verifies that `createUser` calls the email service, without needing a real email server.',
    },
    {
      id: 'fdtg-005',
      topic: 'Unit Tests',
      difficulty: 'medium',
      prompt: 'What is Test-Driven Development (TDD)?',
      options: [
        { id: 'a', text: 'Writing tests after the code is complete' },
        { id: 'b', text: 'Writing a failing test first, then writing the minimum code to make it pass, then refactoring' },
        { id: 'c', text: 'Only writing tests for bug fixes' },
        { id: 'd', text: 'Using a dedicated QA team to test developer code' },
      ],
      correctOptionId: 'b',
      definition: 'Test-Driven Development (TDD) is a development process where you write a failing test before writing the production code, then write just enough code to pass the test, and finally refactor — the "Red-Green-Refactor" cycle.',
      explanation: 'TDD ensures that every piece of code is covered by a test written before the implementation, so the test genuinely validates behavior rather than being retrofitted to passing code. The discipline also drives better design: writing a test first forces you to think about the API before the implementation, leading to more modular, testable code.',
      whyOthersAreWrong: [
        'Option A: Writing tests after code is complete is the traditional (non-TDD) approach; TDD requires tests to be written first, before any production code exists.',
        'Option C: Writing tests only for bug fixes is a minimal regression-testing practice; TDD applies to all new development, not just bug fixes.',
        'Option D: A dedicated QA team describes manual or organizational testing processes; TDD is a developer practice integrated into the coding workflow.',
      ],
      example: 'Red: write `test("divide by zero throws", () => { expect(() => divide(1, 0)).toThrow(); });` — it fails because `divide` does not exist. Green: implement `divide` with a zero check. Refactor: improve the error message and code clarity.',
    },
    {
      id: 'fdtg-006',
      topic: 'Git Basics',
      difficulty: 'medium',
      prompt: 'What does git rebase do?',
      options: [
        { id: 'a', text: 'Creates a new branch from the current commit' },
        { id: 'b', text: 'Moves or re-applies commits from one branch onto another, rewriting commit history' },
        { id: 'c', text: 'Merges two branches and creates a merge commit' },
        { id: 'd', text: 'Reverts the last commit' },
      ],
      correctOptionId: 'b',
      definition: 'Git rebase re-applies a sequence of commits from one branch on top of another branch, effectively rewriting the commit history to produce a linear sequence without merge commits.',
      explanation: 'Instead of creating a merge commit that joins two diverged histories, rebase replays your commits one by one on top of the target branch. This produces a cleaner, linear history that is easier to read with `git log`. However, because it rewrites history, you should never rebase commits that have been pushed to a shared branch.',
      whyOthersAreWrong: [
        'Option A: Creating a new branch is `git branch` or `git checkout -b`; rebase does not create branches.',
        'Option C: Creating a merge commit is what `git merge` does; rebase produces a linear history without merge commits.',
        'Option D: Reverting the last commit is `git revert HEAD` or `git reset HEAD~1`; rebase moves commits, it does not undo them.',
      ],
      example: 'You branch off `main` and make 3 commits. Meanwhile `main` gets 2 new commits. `git rebase main` takes your 3 commits and replays them on top of the latest `main`, as if you branched after those 2 new commits — no merge commit needed.',
    },
    {
      id: 'fdtg-007',
      topic: 'Git Basics',
      difficulty: 'easy',
      prompt: 'What is a merge conflict?',
      options: [
        { id: 'a', text: 'A failed push to a remote repository' },
        { id: 'b', text: 'A situation where two branches have made incompatible changes to the same part of a file' },
        { id: 'c', text: 'An error caused by invalid commit messages' },
        { id: 'd', text: 'A branch that is behind the main branch' },
      ],
      correctOptionId: 'b',
      definition: 'A merge conflict occurs when Git cannot automatically reconcile differences between two branches because both branches modified the same lines of the same file in incompatible ways.',
      explanation: 'When a conflict occurs, Git marks the conflicting sections in the file with `<<<<<<<`, `=======`, and `>>>>>>>` markers. A developer must manually choose which changes to keep (or combine both), then stage the resolved file and complete the merge. Conflicts are normal in collaborative development and are resolved through communication and careful editing.',
      whyOthersAreWrong: [
        'Option A: A failed push is typically a rejection error caused by the remote having commits you do not have locally — solved with `git pull` first.',
        'Option C: Invalid commit messages are a style issue caught by commit message hooks; they are unrelated to merge conflicts.',
        'Option D: A branch behind the main branch needs to be updated (via merge or rebase) but is not itself a conflict — conflicts only arise when incompatible changes exist.',
      ],
      example: 'Alice edits line 5 of `config.ts` in her branch. Bob also edits line 5 in his branch. When merging, Git marks the conflict. Both developers discuss which version (or a combination) to keep, edit the file, run `git add config.ts`, and `git merge --continue`.',
    },
    {
      id: 'fdtg-008',
      topic: 'Git Basics',
      difficulty: 'easy',
      prompt: 'What is git blame used for?',
      options: [
        { id: 'a', text: 'Finding who introduced a bug by identifying the author and commit for each line of a file' },
        { id: 'b', text: 'Reverting a bad commit' },
        { id: 'c', text: 'Deleting a branch from the remote' },
        { id: 'd', text: 'Listing all merge conflicts' },
      ],
      correctOptionId: 'a',
      definition: '`git blame` annotates each line of a file with the commit SHA, author, and date that last modified that line, allowing you to trace when and by whom each line was introduced.',
      explanation: 'Despite its name, `git blame` is not about assigning fault — it is about understanding context. When you find a confusing line of code, `git blame` shows you the commit that introduced it. You can then read the commit message and the associated pull request to understand why that decision was made, which is invaluable for maintaining or modifying legacy code.',
      whyOthersAreWrong: [
        'Option B: Reverting a bad commit is done with `git revert <sha>` or `git reset`.',
        'Option C: Deleting a remote branch is done with `git push origin --delete <branch>`.',
        'Option D: `git blame` shows per-line authorship; listing merge conflicts would be done during a merge operation, not with `git blame`.',
      ],
      example: 'Running `git blame src/auth.ts` shows something like: `abc1234 (Alice 2024-03-12) const TOKEN_EXPIRY = 3600;`. You can then run `git show abc1234` to see the full context of why Alice set that expiry duration.',
    },
    {
      id: 'fdtg-009',
      topic: 'Git Basics',
      difficulty: 'easy',
      prompt: 'What is a good practice for commit messages?',
      options: [
        { id: 'a', text: 'Use a single emoji to describe the change' },
        { id: 'b', text: 'Write a concise, imperative-mood summary describing what the commit does and why' },
        { id: 'c', text: 'Include the full diff in the message' },
        { id: 'd', text: 'Use timestamps as the message' },
      ],
      correctOptionId: 'b',
      definition: 'A good commit message has a concise imperative-mood subject line (e.g., "Add rate limiting to login endpoint") and optionally a body explaining what and why, helping future developers understand the history.',
      explanation: 'Imperative mood ("Fix", "Add", "Remove") matches how Git itself phrases messages ("Merge branch..."). The subject should explain what was done; the body explains why if non-obvious. Good commit messages serve as documentation in `git log`, making it possible to understand changes without reading the diff.',
      whyOthersAreWrong: [
        'Option A: A single emoji conveys no semantic information about what was changed; emojis can supplement a message but cannot replace a description.',
        'Option C: The diff is already stored by Git; including it in the message is redundant and makes the message unreadable.',
        'Option D: Timestamps are already stored automatically with every commit; using a timestamp as the message provides no information about what was done.',
      ],
      example: 'Bad: git commit -m "fix". Good: git commit -m "Fix null pointer in user auth when token is expired" -m "Previously, an expired token caused a crash because validateToken() returned null and callers did not check. Added a null guard."',
    },
    {
      id: 'fdtg-010',
      topic: 'Integration Tests',
      difficulty: 'medium',
      prompt: 'What is integration testing?',
      options: [
        { id: 'a', text: 'Testing a single function in isolation with mocked dependencies' },
        { id: 'b', text: 'Testing how multiple components or services work together to verify their interactions' },
        { id: 'c', text: 'Testing the UI with real users' },
        { id: 'd', text: 'Checking that all unit tests pass in CI' },
      ],
      correctOptionId: 'b',
      definition: 'Integration testing verifies that multiple units or components work correctly together, catching bugs that only emerge at the boundaries between systems — such as database queries, API contracts, or service interactions.',
      explanation: 'Unit tests verify individual pieces in isolation, but they cannot catch integration bugs like mismatched API contracts, incorrect SQL queries, or misconfigured middleware. Integration tests exercise real interactions — connecting to a test database, calling real HTTP handlers — to verify the system works as a whole at the component level.',
      whyOthersAreWrong: [
        'Option A: Testing a single function in isolation with mocks is a unit test; integration tests specifically test the interaction between components.',
        'Option C: Testing the UI with real users is user acceptance testing (UAT); integration tests are automated and focused on component interactions, not user experience.',
        'Option D: CI running all tests is a pipeline step, not a testing type; integration testing is specifically about verifying component interactions.',
      ],
      example: 'An integration test for a REST API endpoint uses a real HTTP request against the running server with a real test database: `const res = await request(app).post("/users").send({ email: "test@example.com" }); expect(res.status).toBe(201);`. This verifies routing, validation, DB write, and response format all together.',
    },
    {
      id: 'fdtg-011',
      topic: 'Unit Tests',
      difficulty: 'medium',
      prompt: 'What should you generally NOT mock in unit tests?',
      options: [
        { id: 'a', text: 'External HTTP APIs' },
        { id: 'b', text: 'Database connections' },
        { id: 'c', text: 'The code under test itself' },
        { id: 'd', text: 'Third-party libraries' },
      ],
      correctOptionId: 'c',
      definition: 'You should never mock the code that the test is supposed to be verifying — mocking the unit under test would mean the test exercises the mock, not the real logic.',
      explanation: 'Mocking exists to isolate the unit under test from its dependencies. If you mock the unit itself (the function or class being tested), your test no longer verifies any real behavior. You can mock anything the unit calls externally (APIs, databases, other services), but the unit\'s own logic must execute for real.',
      whyOthersAreWrong: [
        'Option A: External HTTP APIs should be mocked in unit tests to avoid network calls and to test different response scenarios (success, error, timeout) reliably.',
        'Option B: Database connections should be mocked in unit tests to avoid slow, stateful, external dependencies and to test in isolation.',
        'Option D: Third-party libraries are legitimate to mock when their side effects or I/O behavior needs to be controlled in unit tests.',
      ],
      example: 'Testing `UserService.createUser()`: mock `emailService.send()` (external dependency) and mock `userRepository.save()` (DB). But do NOT mock `UserService.createUser()` itself — that is the thing you are testing. If you mock it, your test proves nothing about its implementation.',
    },
    {
      id: 'fdtg-012',
      topic: 'Unit Tests',
      difficulty: 'easy',
      prompt: 'What is a regression test?',
      options: [
        { id: 'a', text: 'A test that measures performance over time' },
        { id: 'b', text: 'A test that ensures a previously fixed bug has not reappeared' },
        { id: 'c', text: 'A test run only before major releases' },
        { id: 'd', text: 'A test that verifies database migrations' },
      ],
      correctOptionId: 'b',
      definition: 'A regression test is an automated test written after a bug is fixed to verify that the specific bug does not reappear (regress) in future changes.',
      explanation: 'Without a regression test, a bug can silently return when code is refactored or features are added nearby. By writing a test that reproduces the original bug scenario, you permanently guard against that regression. Over time, the regression test suite documents the history of bugs that were found and fixed.',
      whyOthersAreWrong: [
        'Option A: Tests that measure performance over time are performance or benchmark tests; regression tests verify functional correctness of previously-fixed bugs.',
        'Option C: Regression tests should run on every build in CI, not only before major releases — the entire point is early detection of regressions.',
        'Option D: Verifying database migrations is integration testing; regression tests specifically guard against reintroduction of fixed bugs.',
      ],
      example: 'A bug: `calculateDiscount(price, 0)` crashed with a division-by-zero error. After fixing: write `test("zero rate returns full price", () => { expect(calculateDiscount(100, 0)).toBe(100); });`. This test will catch if the zero-check is ever accidentally removed.',
    },
    {
      id: 'fdtg-013',
      topic: 'Git Basics',
      difficulty: 'medium',
      prompt: 'What does git cherry-pick do?',
      options: [
        { id: 'a', text: 'Selects specific files from another branch' },
        { id: 'b', text: 'Applies the changes from a specific commit onto the current branch' },
        { id: 'c', text: 'Squashes multiple commits into one' },
        { id: 'd', text: 'Tags a commit with a version number' },
      ],
      correctOptionId: 'b',
      definition: 'Git cherry-pick applies the changes introduced by a specific commit (or a range of commits) onto the current branch, creating a new commit with the same changes but a different SHA.',
      explanation: 'Cherry-picking is useful when you need to apply a specific bug fix from one branch to another without merging the entire branch. For example, you might cherry-pick a hotfix commit from a feature branch into the `main` branch. It gives you surgical control over which changes to bring across branches.',
      whyOthersAreWrong: [
        'Option A: Selecting specific files from another branch is done with `git checkout <branch> -- <file>` or `git restore --source=<branch> -- <file>`.',
        'Option C: Squashing commits is done interactively with `git rebase -i` or by using `--squash` during a merge.',
        'Option D: Tagging a commit is done with `git tag v1.2.3 <sha>`.',
      ],
      example: 'A critical security fix was committed to `feature/auth` as commit `abc1234`. To apply it to `main`: `git checkout main && git cherry-pick abc1234`. A new commit appears on `main` with the same fix but a new SHA.',
    },
    {
      id: 'fdtg-014',
      topic: 'Unit Tests',
      difficulty: 'easy',
      prompt: 'What does code coverage measure?',
      options: [
        { id: 'a', text: 'The quality of tests' },
        { id: 'b', text: 'The percentage of source code lines (or branches) executed by the test suite' },
        { id: 'c', text: 'The number of bugs found per test run' },
        { id: 'd', text: 'How fast the tests run' },
      ],
      correctOptionId: 'b',
      definition: 'Code coverage measures the proportion of source code (lines, branches, functions, or statements) that is executed by the automated test suite.',
      explanation: 'High coverage tells you what code was executed, not whether it was tested meaningfully. 100% coverage does not mean bug-free code — you could have assertions that do not really verify behavior. Coverage is a useful signal for finding untested areas but should not be the sole measure of test quality. Aim for high coverage on critical paths, not 100% everywhere.',
      whyOthersAreWrong: [
        'Option A: Test quality involves the meaningfulness of assertions, edge case coverage, and test design — none of which are captured by line coverage numbers.',
        'Option C: Coverage tracks code paths exercised, not bugs found; test results (pass/fail) indicate bugs found.',
        'Option D: Test execution speed is measured by the test runner and reported separately; coverage tracks which code was hit, not how long it took.',
      ],
      example: 'Jest reports: "All files | 87.5 | 80 | 100 | 87.5 |" — 87.5% lines, 80% branches. A branch coverage gap might reveal that your `if/else` error path has no test. Tools like Istanbul (included in Jest) and Coverlet (.NET) generate coverage reports.',
    },
    {
      id: 'fdtg-015',
      topic: 'Debugging',
      difficulty: 'easy',
      prompt: 'What is rubber duck debugging?',
      options: [
        { id: 'a', text: 'A tool that automatically finds bugs' },
        { id: 'b', text: 'Explaining your code line-by-line to an inanimate object (or anyone) to find the problem through articulation' },
        { id: 'c', text: 'A pair programming technique' },
        { id: 'd', text: 'Reverting code to an earlier version to find when a bug was introduced' },
      ],
      correctOptionId: 'b',
      definition: 'Rubber duck debugging is the practice of explaining your code in detail to an inanimate listener (classically a rubber duck) — the act of articulating the problem forces you to think through it more carefully and often reveals the bug.',
      explanation: 'When you explain code out loud, you must make every assumption explicit. You often catch the bug mid-sentence because you realize the code does not do what you assumed. This works even without an expert listener because the value comes from the speaker\'s mental process, not feedback. It is a surprisingly effective and free debugging technique.',
      whyOthersAreWrong: [
        'Option A: No tool automatically finds all bugs by rubber ducking; the technique relies on the developer\'s own verbal articulation and self-reflection.',
        'Option C: Pair programming involves two people actively collaborating on code; rubber duck debugging can be done entirely alone.',
        'Option D: Reverting to find when a bug was introduced describes using `git bisect`, a binary-search-based tool for tracking down regressions.',
      ],
      example: 'You have spent 30 minutes on a bug. You start explaining to your rubber duck: "So the function receives an array... and iterates over it... and for each item it calls... oh wait. I\'m passing `items` but the parameter expects `users` and I\'m accessing `.name` which doesn\'t exist on an item object." Bug found.',
    },
  ],
};
