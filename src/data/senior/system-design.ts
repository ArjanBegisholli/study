import type { Test } from '../../types/quiz';

export const seniorSystemDesign: Test = {
  id: 'senior-system-design',
  role: 'senior',
  title: 'System Design',
  description: 'Scalability, reliability, caching strategies, message queues, and distributed systems fundamentals.',
  topics: ['Scalability', 'Reliability', 'Caching', 'Queues', 'CAP Theorem'],
  estimatedMinutes: 30,
  questions: [
    {
      id: 'ssd-001',
      topic: 'Scalability',
      difficulty: 'medium',
      prompt: 'What is the difference between horizontal and vertical scaling?',
      options: [
        { id: 'a', text: 'Horizontal scaling adds more resources to a single machine; vertical adds more machines' },
        { id: 'b', text: 'Horizontal scaling adds more machines; vertical scaling adds more resources (CPU, RAM) to existing machines' },
        { id: 'c', text: 'Horizontal scaling is for databases only; vertical is for application servers' },
        { id: 'd', text: 'There is no meaningful difference between the two approaches' },
      ],
      correctOptionId: 'b',
      definition: 'Horizontal scaling (scaling out) adds more machines to a pool. Vertical scaling (scaling up) adds more CPU, RAM, or storage to an existing machine.',
      explanation: 'Vertical scaling is simpler (no distributed systems complexity) but has a hard hardware ceiling and requires downtime. Horizontal scaling allows virtually unlimited capacity and provides redundancy, but requires stateless services, load balancers, and distributed data management. Modern cloud-native architectures prefer horizontal scaling.',
      whyOthersAreWrong: [
        'Option A: This is the description reversed — "out" (horizontal) adds machines; "up" (vertical) adds resources to one machine.',
        'Option C: Both scaling strategies apply to any layer — databases (read replicas for horizontal, larger instance for vertical), application servers, caches, etc.',
        'Option D: They are fundamentally different strategies with distinct tradeoffs in cost, complexity, and limits.',
      ],
      example: 'A Node.js API becomes slow under load. Vertical scaling: upgrade from 4 to 32 cores on the same machine — quick but expensive and there is a ceiling. Horizontal scaling: run 8 instances behind a load balancer — linear cost, near-infinite ceiling, requires session externalization (Redis).',
    },
    {
      id: 'ssd-002',
      topic: 'Scalability',
      difficulty: 'medium',
      prompt: 'What does a load balancer do?',
      options: [
        { id: 'a', text: 'Increases the processing speed of individual servers' },
        { id: 'b', text: 'Distributes incoming traffic across multiple servers to improve availability and scalability' },
        { id: 'c', text: 'Manages database connection pooling' },
        { id: 'd', text: 'Compresses responses before sending them to clients' },
      ],
      correctOptionId: 'b',
      definition: 'A load balancer is a component that accepts incoming requests and distributes them across a pool of servers according to a distribution algorithm (round-robin, least connections, IP hash, etc.), improving availability and throughput.',
      explanation: 'Load balancers serve two purposes: scalability (spreading load across many servers) and availability (if a server fails, the load balancer stops routing to it and redistributes traffic). They also enable zero-downtime deployments — take a server out of rotation, update it, put it back. Algorithms like least-connections route to the server currently handling the fewest requests.',
      whyOthersAreWrong: [
        'Option A: Load balancers do not change the processing speed of individual servers; they distribute work across them.',
        'Option C: Database connection pooling is a separate concern managed by connection pool libraries (PgPool, HikariCP) or databases themselves.',
        'Option D: Response compression is a web server / CDN feature; load balancers focus on traffic distribution, though some do support SSL termination and header manipulation.',
      ],
      example: 'Three instances of an API behind an nginx load balancer using round-robin: request 1 → server A, request 2 → server B, request 3 → server C, request 4 → server A. If server B crashes, the health check fails and requests are distributed only between A and C.',
    },
    {
      id: 'ssd-003',
      topic: 'Scalability',
      difficulty: 'easy',
      prompt: 'What is the primary purpose of a CDN (Content Delivery Network)?',
      options: [
        { id: 'a', text: 'To host application databases closer to users' },
        { id: 'b', text: 'To cache and serve static assets from servers geographically close to users, reducing latency' },
        { id: 'c', text: 'To load balance API traffic across data centers' },
        { id: 'd', text: 'To encrypt traffic between the client and server' },
      ],
      correctOptionId: 'b',
      definition: 'A CDN (Content Delivery Network) is a geographically distributed network of servers (edge nodes) that cache static content — images, CSS, JS, videos — and serve them from the location closest to each user, minimizing round-trip latency.',
      explanation: 'Serving a 5MB image from a data center in Virginia to a user in Tokyo has 150ms+ latency. A CDN caches the image at an edge node in Tokyo, reducing latency to 5ms. CDNs also reduce origin server load by absorbing the majority of static asset requests. They typically handle cache invalidation via TTLs and explicit purge APIs.',
      whyOthersAreWrong: [
        'Option A: CDNs do not host databases; they cache static assets. Hosting databases near users requires distributed database architectures.',
        'Option C: Load balancing API traffic across data centers is done by global load balancers (AWS Route 53 latency routing, Cloudflare DNS); CDNs primarily serve cached content, not dynamic API traffic.',
        'Option D: TLS/encryption is provided by HTTPS and is separate from CDN geography; CDNs often terminate TLS but their primary value is geographic proximity, not encryption.',
      ],
      example: 'A React app deployed on Vercel/Cloudflare Pages serves the `main.js` bundle from an edge node in the user\'s region. A user in Singapore downloads the bundle in 30ms instead of 200ms from the US origin server. Cache-Control headers control how long the CDN holds the bundle.',
    },
    {
      id: 'ssd-004',
      topic: 'Caching',
      difficulty: 'medium',
      prompt: 'What is cache invalidation and why is it hard?',
      options: [
        { id: 'a', text: 'Removing a cache entirely when the server restarts' },
        { id: 'b', text: 'Determining when cached data is stale and must be updated or evicted — it is hard because distributed caches may serve stale data if not carefully managed' },
        { id: 'c', text: 'Preventing the cache from growing too large' },
        { id: 'd', text: 'Setting the TTL (time to live) on cache entries' },
      ],
      correctOptionId: 'b',
      definition: 'Cache invalidation is the process of removing or updating cached entries when the underlying data changes. It is notoriously difficult because distributed caches can hold stale data, and coordinating invalidation across multiple cache nodes adds consistency challenges.',
      explanation: 'Phil Karlton famously said: "There are only two hard things in Computer Science: cache invalidation and naming things." The difficulty lies in knowing when data is stale and ensuring all caches holding that data are simultaneously updated. Solutions include TTL-based expiry (simple but can serve stale data during the TTL window), event-driven invalidation (complex, consistent), and write-through caching (always up to date, write overhead).',
      whyOthersAreWrong: [
        'Option A: Flushing the entire cache on restart is a crude approach unrelated to the nuanced challenge of invalidating specific entries when their source data changes.',
        'Option C: Preventing cache overflow is cache eviction policy (LRU, LFU, TTL expiry) — a related but distinct concern from invalidation (which is about data freshness).',
        'Option D: Setting TTLs is one invalidation strategy, but TTL alone cannot guarantee fresh data on every read — you may serve stale data for the TTL duration after a source update.',
      ],
      example: 'A product page is cached for 60 seconds. The price drops 10 seconds after caching. For the next 50 seconds, users see the old price. Fix options: shorter TTL (more DB load), write-through cache (invalidate on price update), or stale-while-revalidate pattern (serve stale, update in background).',
    },
    {
      id: 'ssd-005',
      topic: 'Queues',
      difficulty: 'medium',
      prompt: 'What are the main benefits of using a message queue?',
      options: [
        { id: 'a', text: 'They eliminate the need for databases' },
        { id: 'b', text: 'They decouple producers from consumers, enable async processing, smooth traffic spikes, and improve resilience when consumers are temporarily unavailable' },
        { id: 'c', text: 'They provide faster request/response than HTTP' },
        { id: 'd', text: 'They automatically scale the consuming service' },
      ],
      correctOptionId: 'b',
      definition: 'Message queues provide asynchronous communication between services, acting as a buffer where producers publish messages and consumers process them independently, decoupling the two sides in time and scale.',
      explanation: 'Without queues, a producer calling a consumer synchronously couples their availability and throughput. With a queue: if the consumer is slow, messages buffer. If the consumer crashes, messages are retained for retry. A traffic spike results in a queue depth increase rather than consumer overload. Services are also physically decoupled — they do not need to know each other\'s addresses.',
      whyOthersAreWrong: [
        'Option A: Message queues do not replace databases; they handle message passing between services but have no role in persistent data storage.',
        'Option C: Message queues add latency for the queue round-trip; they are not faster than direct HTTP for synchronous request/response. Their value is async processing, not speed.',
        'Option D: Auto-scaling consumers based on queue depth requires additional tooling (Kubernetes KEDA, auto-scaling groups); the queue itself does not scale the consumer.',
      ],
      example: 'An e-commerce order placement: the API instantly acknowledges the order and publishes `order.created` to a queue (fast response to user). Background workers consume messages to run fraud checks, charge payment, update inventory, and send email — at their own pace, resilient to individual worker failures.',
    },
    {
      id: 'ssd-006',
      topic: 'Queues',
      difficulty: 'medium',
      prompt: 'What is the difference between pub/sub messaging and request/reply?',
      options: [
        { id: 'a', text: 'Pub/sub is synchronous; request/reply is asynchronous' },
        { id: 'b', text: 'In pub/sub a publisher broadcasts to many subscribers without knowing who they are; in request/reply the caller waits for a specific response' },
        { id: 'c', text: 'Request/reply is only for internal services; pub/sub is for external clients' },
        { id: 'd', text: 'They are architecturally identical but use different protocols' },
      ],
      correctOptionId: 'b',
      definition: 'Pub/sub (publish-subscribe) is a messaging pattern where publishers send messages to a topic and any number of subscribers receive them — publishers and subscribers are decoupled. Request/reply is a synchronous or async pattern where a caller sends a message and awaits a specific response from one recipient.',
      explanation: 'Pub/sub enables fan-out (one event → many consumers) and decoupling (publisher does not need to know subscribers exist). Request/reply is appropriate for queries or operations where the caller needs the result before proceeding. Mixing patterns is common: a service uses pub/sub for events and request/reply for synchronous queries.',
      whyOthersAreWrong: [
        'Option A: Pub/sub is asynchronous; request/reply can be synchronous (HTTP) or asynchronous (async request with correlation ID). This is reversed.',
        'Option C: Both patterns are used for internal service communication; the choice depends on whether you need fan-out and decoupling (pub/sub) or a single response (request/reply).',
        'Option D: They are architecturally distinct patterns with different semantics — one-to-many broadcasting vs one-to-one response semantics.',
      ],
      example: 'Pub/sub: `order.completed` event published → `EmailService`, `AnalyticsService`, and `LoyaltyService` all subscribe and process independently. Request/reply: `CheckoutService` calls `InventoryService.reserve(item, qty)` and waits for `{ success: true, reservationId: "xyz" }` before proceeding.',
    },
    {
      id: 'ssd-007',
      topic: 'CAP Theorem',
      difficulty: 'hard',
      prompt: 'What does the CAP theorem state?',
      options: [
        { id: 'a', text: 'A distributed system can always be Consistent, Available, and Partition-tolerant simultaneously' },
        { id: 'b', text: 'A distributed system can guarantee at most two of: Consistency, Availability, and Partition Tolerance — not all three' },
        { id: 'c', text: 'CAP stands for Caching, Availability, and Performance' },
        { id: 'd', text: 'A distributed system must choose between availability and performance' },
      ],
      correctOptionId: 'b',
      definition: 'The CAP theorem (Brewer\'s theorem) states that a distributed data store can provide at most two of three guarantees: Consistency (every read returns the latest write), Availability (every request receives a response), and Partition Tolerance (the system continues operating despite network partitions).',
      explanation: 'In practice, network partitions cannot be avoided in real distributed systems, so the real choice is between consistency and availability when a partition occurs. CP systems (e.g., HBase, Zookeeper) return errors during partitions to maintain consistency. AP systems (e.g., Cassandra, DynamoDB) continue serving requests during partitions but may return stale data. Most systems offer tunable consistency.',
      whyOthersAreWrong: [
        'Option A: This contradicts the theorem — the entire point is that all three cannot be guaranteed simultaneously in the presence of network partitions.',
        'Option C: CAP stands for Consistency, Availability, and Partition Tolerance — not Caching, Availability, and Performance.',
        'Option D: CAP is about the consistency/availability tradeoff during network partitions, not about performance tradeoffs.',
      ],
      example: 'Your distributed database experiences a network partition between two data centers. CP choice (MongoDB default): refuse writes on the minority partition — consistent but unavailable. AP choice (Cassandra): accept writes on both sides, resolve conflicts later — available but temporarily inconsistent.',
    },
    {
      id: 'ssd-008',
      topic: 'CAP Theorem',
      difficulty: 'hard',
      prompt: 'In the context of CAP, what is eventual consistency?',
      options: [
        { id: 'a', text: 'Data is consistent after a scheduled sync window' },
        { id: 'b', text: 'Replicas may return stale data temporarily but will converge to the same value given no new updates' },
        { id: 'c', text: 'Each write is confirmed by a majority of nodes before succeeding' },
        { id: 'd', text: 'Reads always return the most recent write' },
      ],
      correctOptionId: 'b',
      definition: 'Eventual consistency is a consistency model where all replicas of a distributed data item will eventually converge to the same value, given that no new updates are made — but at any given moment, replicas may return different (stale) values.',
      explanation: 'Eventual consistency is the AP side of CAP: prioritize availability over strict consistency. Writes are accepted and propagated asynchronously. For many use cases (social media counts, shopping carts, DNS), brief inconsistency is acceptable. For financial transactions or inventory counts, stronger consistency (quorum reads/writes or linearizability) is required.',
      whyOthersAreWrong: [
        'Option A: A scheduled sync window is a specific implementation detail; eventual consistency is a model property — convergence is guaranteed given quiescence, not on a schedule.',
        'Option C: Majority node confirmation before success describes strong consistency (quorum consistency, used by Paxos/Raft-based systems like etcd); this is the opposite of eventual consistency.',
        'Option D: Reads always returning the most recent write is linearizability (the strongest consistency model); eventual consistency explicitly allows stale reads.',
      ],
      example: 'You "like" a post on a social network. The like is recorded on the nearest replica but takes 500ms to propagate to replicas worldwide. A friend in another region sees 99 likes momentarily, then 100 likes after propagation. Eventual consistency: correct, eventually.',
    },
    {
      id: 'ssd-009',
      topic: 'Scalability',
      difficulty: 'hard',
      prompt: 'What is database sharding?',
      options: [
        { id: 'a', text: 'Replicating a database across multiple read replicas' },
        { id: 'b', text: 'Horizontally partitioning a database across multiple machines by splitting data into shards based on a key' },
        { id: 'c', text: 'Archiving old data to cheaper storage' },
        { id: 'd', text: 'Encrypting individual database columns' },
      ],
      correctOptionId: 'b',
      definition: 'Database sharding is horizontal partitioning of data across multiple independent database instances (shards), where each shard holds a subset of rows determined by a shard key — enabling the database to scale beyond a single machine\'s capacity.',
      explanation: 'A shard key (e.g., user_id % n) determines which shard holds each record. Each shard is an independent database instance, so read and write load is distributed. Sharding adds operational complexity: cross-shard queries require scatter-gather, rebalancing when adding shards is complex, and distributed transactions span multiple shards. It is a last resort when vertical scaling and read replicas are insufficient.',
      whyOthersAreWrong: [
        'Option A: Replicating across read replicas is replication, not sharding; replicas hold the same data on multiple machines for read scalability. Sharding splits different data across machines.',
        'Option C: Archiving old data is data lifecycle management (tiered storage, cold/warm/hot data); sharding keeps active data distributed across machines for write scalability.',
        'Option D: Column encryption is a security feature; sharding is a horizontal scalability architecture.',
      ],
      example: 'A messaging app with 1 billion users. Shard by user_id: users 0-249M → shard 1, 250M-499M → shard 2, etc. Each shard is an independent PostgreSQL instance handling 250M users worth of load. A message from user 100M is stored and queried only on shard 1.',
    },
    {
      id: 'ssd-010',
      topic: 'Reliability',
      difficulty: 'medium',
      prompt: 'What is the purpose of read replicas in a database?',
      options: [
        { id: 'a', text: 'To provide a backup in case the primary fails' },
        { id: 'b', text: 'To offload read traffic from the primary database, improving read scalability and reducing load on the primary' },
        { id: 'c', text: 'To handle write traffic when the primary is overloaded' },
        { id: 'd', text: 'To store data in a different geographic region for compliance' },
      ],
      correctOptionId: 'b',
      definition: 'Read replicas are database instances that asynchronously replicate data from the primary database and serve read-only queries, distributing read load across multiple instances without affecting write performance on the primary.',
      explanation: 'Most applications have many more reads than writes. Adding read replicas allows read queries to be distributed across multiple instances. The primary handles all writes and replicates changes to replicas asynchronously. Applications route writes to the primary and reads to replicas. The tradeoff is replication lag — replicas may be slightly behind the primary.',
      whyOthersAreWrong: [
        'Option A: Failover to a replica in case of primary failure is possible (replica promotion), but it is not the primary purpose of read replicas. Backups are separate (point-in-time backups, snapshots).',
        'Option C: Read replicas cannot accept writes from the application; they are strictly read-only. Handling write overflow requires sharding or write scaling strategies.',
        'Option D: Geographic data placement for compliance uses regional deployments or geo-replication configurations; read replicas are specifically for distributing read load, not regulatory compliance.',
      ],
      example: 'An analytics dashboard runs complex `SELECT` queries that take 2 seconds each. Routing these to a read replica instead of the primary prevents these expensive queries from slowing down user-facing write operations. The primary handles `INSERT`/`UPDATE`; 3 replicas share the read load.',
    },
    {
      id: 'ssd-011',
      topic: 'Reliability',
      difficulty: 'hard',
      prompt: 'What is the circuit breaker pattern?',
      options: [
        { id: 'a', text: 'A method of encrypting network connections' },
        { id: 'b', text: 'A pattern that stops making requests to a failing service and returns a fallback response, periodically checking if the service has recovered' },
        { id: 'c', text: 'A load balancing strategy that routes traffic based on latency' },
        { id: 'd', text: 'A retry mechanism with exponential backoff' },
      ],
      correctOptionId: 'b',
      definition: 'The circuit breaker pattern monitors calls to a remote service and "trips" (opens) when failures exceed a threshold, fast-failing subsequent calls and returning a fallback instead of waiting for timeouts — preventing cascading failures across services.',
      explanation: 'Like an electrical circuit breaker, it has three states: Closed (normal, pass requests through), Open (tripped — fast-fail all requests with fallback), and Half-Open (periodically allow a test request to check if the service recovered, close if successful). This prevents a slow/failing service from causing timeouts across the entire system.',
      whyOthersAreWrong: [
        'Option A: Network encryption is TLS/SSL; the circuit breaker is an application-level resilience pattern unrelated to transport security.',
        'Option C: Latency-based routing is a load balancing strategy (weighted routing); circuit breakers are about failure detection and fast-failing, not traffic distribution.',
        'Option D: Exponential backoff is a retry strategy; circuit breakers are complementary but distinct — they stop retrying entirely during the Open state, while retries attempt to redo individual failed operations.',
      ],
      example: 'OrderService calls PaymentService. PaymentService starts timing out. Without circuit breaker: 1000 pending HTTP connections, thread pool exhausted, OrderService crashes. With circuit breaker: after 5 failures, "open" — all calls return a fallback error immediately. PaymentService gets time to recover without being hammered.',
    },
    {
      id: 'ssd-012',
      topic: 'Reliability',
      difficulty: 'hard',
      prompt: 'What is the bulkhead pattern in distributed systems?',
      options: [
        { id: 'a', text: 'A database isolation level' },
        { id: 'b', text: 'Isolating components into separate pools so a failure in one component does not drain resources and cause cascading failures in others' },
        { id: 'c', text: 'A method of encrypting inter-service communication' },
        { id: 'd', text: 'A load balancing strategy that routes traffic to the least-loaded server' },
      ],
      correctOptionId: 'b',
      definition: 'The bulkhead pattern (named after ship bulkheads that isolate flooding to one compartment) partitions system resources (thread pools, connection pools, rate limits) by consumer or feature, so that one misbehaving component cannot exhaust shared resources and bring down the whole system.',
      explanation: 'Without bulkheads, if a slow downstream service causes requests to queue up, they exhaust a shared thread pool and block all other requests — including healthy ones. With bulkheads, each downstream service gets a dedicated thread pool or connection pool. Exhaustion in one pool does not affect others.',
      whyOthersAreWrong: [
        'Option A: Database isolation levels (READ COMMITTED, SERIALIZABLE) are about transaction visibility in SQL databases; the bulkhead pattern is an architectural resilience pattern for distributed systems.',
        'Option C: Inter-service encryption is mTLS / service mesh security; bulkheads are about resource partitioning for resilience, not security.',
        'Option D: Least-loaded routing is a load balancing algorithm; bulkheads are about resource isolation between consumers, not traffic distribution.',
      ],
      example: 'An API calls three services: Search (high traffic), Recommendations (medium), Notifications (low). Bulkheads: Search gets a thread pool of 50, Recommendations 20, Notifications 5. If Recommendations service stalls and its pool fills up, Search and Notifications continue operating normally.',
    },
    {
      id: 'ssd-013',
      topic: 'Reliability',
      difficulty: 'medium',
      prompt: 'What does it mean for an operation to be idempotent?',
      options: [
        { id: 'a', text: 'The operation can only be called once' },
        { id: 'b', text: 'Calling the operation multiple times with the same input produces the same result as calling it once, with no additional side effects' },
        { id: 'c', text: 'The operation always completes in constant time' },
        { id: 'd', text: 'The operation returns a unique result each time it is called' },
      ],
      correctOptionId: 'b',
      definition: 'An idempotent operation produces the same result and leaves the system in the same state whether it is executed once or many times with the same inputs.',
      explanation: 'Idempotency is critical for distributed systems where network failures can cause retries. A non-idempotent payment charge retried after a timeout would charge the customer twice. An idempotent implementation (keyed on a unique idempotency key) safely handles retries — the second call returns the cached result of the first without re-charging. PUT and DELETE are idempotent; POST typically is not.',
      whyOthersAreWrong: [
        'Option A: Idempotent operations can be called any number of times safely; the point is that multiple calls are equivalent to one call — not that they must be limited to one.',
        'Option C: Constant time (O(1)) is a time complexity property; idempotency is about side effects and result consistency, not execution speed.',
        'Option D: Returning a unique result each time describes a non-idempotent operation (like creating a new UUID); idempotency requires consistent results across identical calls.',
      ],
      example: 'Idempotent: `PUT /users/42 { "name": "Alice" }` — calling it 10 times still results in user 42 having name "Alice". Non-idempotent: `POST /charges { "amount": 100, "card": "..." }` — calling it 10 times creates 10 charges. Fix: `POST /charges` with an `Idempotency-Key` header.',
    },
    {
      id: 'ssd-014',
      topic: 'Queues',
      difficulty: 'hard',
      prompt: 'What is the saga pattern used for?',
      options: [
        { id: 'a', text: 'Managing long-running background jobs' },
        { id: 'b', text: 'Handling distributed transactions across multiple services by using a series of local transactions and compensating actions on failure' },
        { id: 'c', text: 'Routing messages in a pub/sub system' },
        { id: 'd', text: 'Scaling message queues across data centers' },
      ],
      correctOptionId: 'b',
      definition: 'The Saga pattern manages distributed transactions across multiple microservices by breaking the transaction into a sequence of local transactions, each publishing an event or message; if a step fails, compensating transactions are executed to undo the completed steps.',
      explanation: 'Traditional ACID transactions cannot span microservices with separate databases. Sagas replace the global transaction with a series of local transactions. If step 3 of 5 fails, compensating transactions roll back steps 2 and 1. Sagas can be orchestrated (a central coordinator calls each service) or choreographed (services react to events). The tradeoff is eventual consistency and compensation complexity.',
      whyOthersAreWrong: [
        'Option A: Long-running background jobs are managed by job queues (Bull, Celery, Hangfire); while sagas involve message queues, their specific purpose is distributed transaction management.',
        'Option C: Message routing in pub/sub is handled by the message broker\'s topic/subscription configuration; the Saga pattern uses events to coordinate transaction steps.',
        'Option D: Queue scaling is an infrastructure concern for platforms like Kafka or RabbitMQ; the Saga pattern is an application-level transaction management strategy.',
      ],
      example: 'E-commerce order: (1) Reserve inventory → (2) Charge payment → (3) Ship order. If payment fails: compensating transaction releases the inventory reservation. If shipping fails: compensating transaction refunds the payment and releases inventory. Each step is a local transaction with its own compensating action.',
    },
    {
      id: 'ssd-015',
      topic: 'Caching',
      difficulty: 'medium',
      prompt: 'What is a write-through cache strategy?',
      options: [
        { id: 'a', text: 'All writes go directly to the database, bypassing the cache' },
        { id: 'b', text: 'Data is written to the cache and the database simultaneously, ensuring the cache is always consistent with the database' },
        { id: 'c', text: 'Data is written only to the cache and lazily persisted to the database' },
        { id: 'd', text: 'The cache is rebuilt from the database on every write' },
      ],
      correctOptionId: 'b',
      definition: 'Write-through caching writes data to the cache and the underlying database synchronously on every write operation, ensuring the cache and database are always in sync at the cost of slightly higher write latency.',
      explanation: 'Write-through eliminates the possibility of serving stale data (the cache always has the latest) at the cost of requiring both a cache write and a DB write per update. Contrast with write-behind (write to cache, persist to DB asynchronously — lower latency but risk of data loss on cache failure) and cache-aside (application manages population/invalidation explicitly).',
      whyOthersAreWrong: [
        'Option A: Bypassing the cache on writes is the cache-aside (lazy loading) pattern where the cache is populated on reads and invalidated on writes.',
        'Option C: Writing only to cache with lazy database persistence is the write-behind (write-back) strategy — it improves write performance but risks data loss if the cache fails before the async write.',
        'Option D: Rebuilding the entire cache on every write would be catastrophically slow and is not a recognized caching strategy.',
      ],
      example: 'A product inventory system uses write-through caching with Redis. When a purchase decrements stock: `db.update(productId, newCount); cache.set(productId, newCount);`. Both happen in the same transaction. Subsequent reads hit cache and always see current inventory levels.',
    },
  ],
};
