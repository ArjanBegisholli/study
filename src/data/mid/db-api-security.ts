import type { Test } from '../../types/quiz';

export const midDbApiSecurity: Test = {
  id: 'mid-db-api-security',
  role: 'mid',
  title: 'Databases, APIs & Security',
  description: 'Schema design, query optimization, authentication, authorization, and common security vulnerabilities.',
  topics: ['Schema Design', 'Indexing', 'Query Optimization', 'Auth/Authz', 'Security'],
  estimatedMinutes: 25,
  questions: [
    {
      id: 'mdas-001',
      topic: 'Schema Design',
      difficulty: 'medium',
      prompt: 'What is database normalization?',
      options: [
        { id: 'a', text: 'Compressing the database to save storage' },
        { id: 'b', text: 'Organizing tables to reduce data redundancy and improve data integrity by eliminating duplicate data' },
        { id: 'c', text: 'Adding indexes to every column' },
        { id: 'd', text: 'Converting data to a standard format before storage' },
      ],
      correctOptionId: 'b',
      definition: 'Database normalization is the process of structuring a relational database schema to reduce data redundancy and improve data integrity by decomposing tables according to normal form rules (1NF, 2NF, 3NF, etc.).',
      explanation: 'Unnormalized data stores the same information in multiple places, causing update anomalies (changing a customer\'s address requires updating every order row). Normalization separates data into well-defined tables with relationships. However, normalized databases often require JOINs for queries; some systems deliberately denormalize for read performance.',
      whyOthersAreWrong: [
        'Option A: Database compression is a storage engine feature separate from schema design; normalization changes how data is organized across tables, not its storage format.',
        'Option C: Adding indexes affects query performance; normalization determines table structure and relationships.',
        'Option D: Standardizing data formats (e.g., date formats) is data quality work; normalization is about eliminating redundancy in the schema structure.',
      ],
      example: 'Unnormalized: `orders(id, product_name, product_price, customer_name, customer_email)` — customer data repeated per order. Normalized: `customers(id, name, email)`, `products(id, name, price)`, `orders(id, customer_id, product_id)`. Update customer email once, not on every order.',
    },
    {
      id: 'mdas-002',
      topic: 'Indexing',
      difficulty: 'medium',
      prompt: 'When would you use a composite index?',
      options: [
        { id: 'a', text: 'When you want to index all columns of a table' },
        { id: 'b', text: 'When queries frequently filter or sort on multiple columns together, a composite index on those columns speeds up those queries' },
        { id: 'c', text: 'When a table has no primary key' },
        { id: 'd', text: 'To replace a foreign key constraint' },
      ],
      correctOptionId: 'b',
      definition: 'A composite index (also called a multi-column index) is an index on two or more columns, optimized for queries that filter or sort by those columns together.',
      explanation: 'A composite index on `(user_id, created_at)` supports queries like `WHERE user_id = 5 ORDER BY created_at DESC` far better than two separate single-column indexes. The leftmost prefix rule applies: a composite index on `(a, b, c)` can serve queries on `a`, `(a, b)`, or `(a, b, c)` but not on `b` alone or `c` alone.',
      whyOthersAreWrong: [
        'Option A: Indexing every column is an anti-pattern; each index adds write overhead and storage cost. Indexes should be selective and query-driven.',
        'Option C: A missing primary key is a schema design issue; composite indexes are for query performance optimization on any table, regardless of whether it has a primary key.',
        'Option D: Foreign key constraints enforce referential integrity; indexes improve query performance. While foreign key columns often benefit from indexes, composite indexes are a performance optimization, not a constraint replacement.',
      ],
      example: 'An audit log table frequently queried as `SELECT * FROM audit_logs WHERE tenant_id = 5 AND event_type = "login" AND created_at > NOW() - INTERVAL "30 days"`. A composite index on `(tenant_id, event_type, created_at)` makes this query fast.',
    },
    {
      id: 'mdas-003',
      topic: 'Query Optimization',
      difficulty: 'medium',
      prompt: 'What is the N+1 query problem?',
      options: [
        { id: 'a', text: 'A query that returns one more row than expected' },
        { id: 'b', text: 'When fetching a list of N items and then making an individual query for each item, resulting in N+1 total queries instead of 1 or 2' },
        { id: 'c', text: 'A bug in SQL JOINs that duplicates rows' },
        { id: 'd', text: 'When a query plan uses nested loops on two tables' },
      ],
      correctOptionId: 'b',
      definition: 'The N+1 query problem occurs when code fetches a list of N parent records (1 query) and then issues a separate database query for each record to fetch its related data (N queries), totaling N+1 round trips.',
      explanation: 'N+1 is a common ORM pitfall. Fetching 100 blog posts and then loading comments for each in a loop results in 101 database queries instead of 2. Solutions include eager loading (using JOIN or IN clause), data loaders (batching N individual queries into one IN query), or select queries with explicit JOINs.',
      whyOthersAreWrong: [
        'Option A: A query returning one extra row is an off-by-one data bug; the N+1 problem is specifically about the number of database round trips, not row counts.',
        'Option C: Duplicate rows from JOINs (Cartesian products) are a separate SQL JOIN issue — they cause too many rows, not too many queries.',
        'Option D: Nested loop joins are an internal query plan execution strategy chosen by the query optimizer; N+1 is an application-level pattern of issuing many queries from code.',
      ],
      example: 'ORM code that triggers N+1: `const posts = await Post.findAll(); for (const post of posts) { const comments = await post.getComments(); }` — 1 + N queries. Fix with eager loading: `const posts = await Post.findAll({ include: [Comment] });` — 1 or 2 queries total.',
    },
    {
      id: 'mdas-004',
      topic: 'Security',
      difficulty: 'easy',
      prompt: 'How do prepared statements prevent SQL injection?',
      options: [
        { id: 'a', text: 'They encrypt the SQL query before sending it' },
        { id: 'b', text: 'They separate the SQL structure from the user-supplied data so data is treated as values, not executable code' },
        { id: 'c', text: 'They validate that inputs are alphanumeric only' },
        { id: 'd', text: 'They run queries in a sandbox' },
      ],
      correctOptionId: 'b',
      definition: 'Prepared statements (parameterized queries) send the SQL template and user input to the database separately. The database compiles the SQL structure first, then applies the parameters as data — preventing injected SQL from being executed as code.',
      explanation: 'In SQL injection, an attacker includes SQL syntax in user input (e.g., `\'; DROP TABLE users; --`). With prepared statements, the input is bound as a parameter after the query is compiled. The database treats it as a string value, not SQL commands. Even if the input contains SQL syntax, it cannot alter the query structure.',
      whyOthersAreWrong: [
        'Option A: Prepared statements do not encrypt queries; encryption is a transport-layer concern (TLS/SSL). The protection comes from separating code from data.',
        'Option C: Input validation (accepting only alphanumeric characters) is a complementary defense but not what prepared statements do; they protect by structural separation, not content filtering.',
        'Option D: Database sandboxing is a separate security feature; prepared statements work at the SQL compilation level by separating structure from parameters.',
      ],
      example: 'Vulnerable: `query("SELECT * FROM users WHERE email = \'" + userInput + "\'")`. Safe: `query("SELECT * FROM users WHERE email = ?", [userInput])`. If `userInput` is `" OR 1=1 --"`, the vulnerable version executes it as SQL; the prepared statement treats it as a literal string value.',
    },
    {
      id: 'mdas-005',
      topic: 'Auth/Authz',
      difficulty: 'medium',
      prompt: 'What are the three parts of a JWT (JSON Web Token)?',
      options: [
        { id: 'a', text: 'Header, payload, and signature' },
        { id: 'b', text: 'Token, secret, and expiry' },
        { id: 'c', text: 'Username, role, and hash' },
        { id: 'd', text: 'Algorithm, claims, and certificate' },
      ],
      correctOptionId: 'a',
      definition: 'A JWT consists of three Base64URL-encoded parts separated by dots: the Header (metadata including algorithm), the Payload (claims like user ID and expiry), and the Signature (cryptographic proof of integrity).',
      explanation: 'The header specifies the signing algorithm (e.g., HS256). The payload contains claims — registered (iss, exp, sub), public, and private. The signature is computed from header + payload using a secret or private key. Servers verify the signature to trust the claims without a database lookup, enabling stateless authentication.',
      whyOthersAreWrong: [
        'Option B: "Token, secret, and expiry" confuses the JWT structure with the concepts used to create it; the secret is used to generate the signature, not a separate JWT section.',
        'Option C: Username, role, and hash are possible claim values within the payload; they are not the three structural sections of a JWT.',
        'Option D: Algorithm is part of the header, claims are in the payload — these are two of the three parts but "certificate" is not a standard JWT section (public-key JWT uses the key reference, not the full certificate in the JWT itself).',
      ],
      example: 'A JWT looks like: `eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJ1c2VyLTQyIiwiZXhwIjoxNzAwMDAwMDAwfQ.sig`. Decode the middle part: `{ "sub": "user-42", "exp": 1700000000 }`. The server verifies the signature matches to trust these claims.',
    },
    {
      id: 'mdas-006',
      topic: 'Auth/Authz',
      difficulty: 'hard',
      prompt: 'What is the OAuth 2.0 flow for a web application?',
      options: [
        { id: 'a', text: 'The client sends the username and password to the resource server directly' },
        { id: 'b', text: 'The user is redirected to an authorization server, grants permission, and the app receives an authorization code exchanged for an access token' },
        { id: 'c', text: 'The API key is embedded in each request header' },
        { id: 'd', text: 'The resource server issues tokens after verifying the client secret' },
      ],
      correctOptionId: 'b',
      definition: 'OAuth 2.0 Authorization Code Flow: the app redirects the user to an authorization server; the user authenticates and grants consent; the server redirects back with an authorization code; the app exchanges the code (plus client secret) for access and refresh tokens.',
      explanation: 'The Authorization Code Flow is the most secure OAuth 2.0 flow for web applications because the access token is never exposed to the browser — it is exchanged server-to-server. The short-lived authorization code is only useful to a party that also knows the client secret. PKCE extends this for single-page and mobile apps that cannot safely store secrets.',
      whyOthersAreWrong: [
        'Option A: Sending credentials directly to the resource server is the Resource Owner Password Credentials grant, a deprecated flow not recommended because the app handles the user\'s password.',
        'Option C: API keys are a simpler authentication mechanism for server-to-server calls; OAuth 2.0 is specifically about delegated user authorization.',
        'Option D: The resource server validates tokens but does not issue them; the authorization server issues tokens after the authorization code exchange.',
      ],
      example: 'Logging in with Google on a website: your browser redirects to `accounts.google.com/auth?...`; you log in and approve; Google redirects back to your app with `?code=abc123`; the app server calls Google\'s token endpoint with the code and client secret; Google returns an access token for the user\'s data.',
    },
    {
      id: 'mdas-007',
      topic: 'Security',
      difficulty: 'easy',
      prompt: 'Why should passwords be hashed with bcrypt rather than MD5 or SHA-1?',
      options: [
        { id: 'a', text: 'bcrypt produces shorter hashes' },
        { id: 'b', text: 'bcrypt is intentionally slow and includes a salt, making brute-force and rainbow table attacks infeasible' },
        { id: 'c', text: 'MD5 and SHA-1 are not supported in modern databases' },
        { id: 'd', text: 'bcrypt is a symmetric encryption algorithm that can reverse the hash' },
      ],
      correctOptionId: 'b',
      definition: 'bcrypt is a password hashing algorithm designed to be computationally slow (via a configurable work factor) and includes a random salt per password, making mass brute-force and precomputed rainbow table attacks infeasible.',
      explanation: 'MD5 and SHA-1 are fast, general-purpose hash functions — fast is bad for passwords because attackers can compute billions of hashes per second to try common passwords. bcrypt\'s work factor can be increased as hardware gets faster. Its built-in salt means identical passwords produce different hashes, defeating rainbow tables.',
      whyOthersAreWrong: [
        'Option A: bcrypt produces longer hashes (60 characters), not shorter; length does not determine security.',
        'Option C: MD5 and SHA-1 are still widely supported in databases; the reason to avoid them for passwords is their speed, not availability.',
        'Option D: bcrypt is a one-way hash, not reversible — this is a key security property. If it could be reversed, it would be useless for password storage.',
      ],
      example: '`const hash = await bcrypt.hash(password, 12);` — the `12` is the work factor (2^12 = 4096 rounds). Verification: `const match = await bcrypt.compare(inputPassword, storedHash);`. Even if the database is breached, cracking bcrypt hashes is computationally impractical.',
    },
    {
      id: 'mdas-008',
      topic: 'Security',
      difficulty: 'easy',
      prompt: 'Why is HTTPS important for APIs?',
      options: [
        { id: 'a', text: 'It makes APIs respond faster' },
        { id: 'b', text: 'It encrypts data in transit, preventing eavesdropping and man-in-the-middle attacks' },
        { id: 'c', text: 'It eliminates the need for authentication' },
        { id: 'd', text: 'It compresses request and response bodies' },
      ],
      correctOptionId: 'b',
      definition: 'HTTPS (HTTP over TLS) encrypts the connection between client and server, ensuring that request/response data including headers, cookies, and bodies cannot be read or tampered with by network intermediaries.',
      explanation: 'Without HTTPS, API tokens, session cookies, and user data are transmitted in plain text. Any network intermediary (ISP, WiFi router, proxy) can read or modify the traffic — enabling credential theft and session hijacking. HTTPS also authenticates the server via certificates, preventing attackers from impersonating legitimate servers.',
      whyOthersAreWrong: [
        'Option A: TLS adds a small overhead for the handshake; HTTPS does not make APIs faster — it adds a small latency cost that is usually negligible compared to the security benefit.',
        'Option C: HTTPS handles transport security (encryption in transit); authentication (verifying who you are) is a separate application-level concern that HTTPS does not replace.',
        'Option D: HTTP/2 and modern TLS do support compression, but that is separate from the core security purpose of HTTPS, which is encryption and server authentication.',
      ],
      example: 'Without HTTPS, an attacker on the same coffee-shop WiFi can intercept `Authorization: Bearer eyJhbGci...` from your API request and use that token to impersonate you. With HTTPS, all traffic is encrypted and the token is invisible to network observers.',
    },
    {
      id: 'mdas-009',
      topic: 'Security',
      difficulty: 'medium',
      prompt: 'What is a CORS (Cross-Origin Resource Sharing) attack vector?',
      options: [
        { id: 'a', text: 'Injecting SQL through CORS headers' },
        { id: 'b', text: "A browser security issue where a malicious site tries to make requests to another origin on behalf of the user — CORS headers define what's allowed" },
        { id: 'c', text: 'A type of DDoS attack using cross-origin requests' },
        { id: 'd', text: 'Stealing cookies by changing the Origin header' },
      ],
      correctOptionId: 'b',
      definition: 'CORS is a browser mechanism that controls cross-origin requests. A misconfigured CORS policy (e.g., `Access-Control-Allow-Origin: *` with credentials) can allow malicious sites to make authenticated requests to your API on behalf of a logged-in user.',
      explanation: 'Without CORS restrictions, a malicious site could embed script that calls `https://yourbank.com/api/transfer` with the victim\'s cookies attached. Browsers enforce the Same-Origin Policy by default; CORS headers selectively relax this. Misconfiguring CORS to allow all origins with credentials is as dangerous as having no CORS at all.',
      whyOthersAreWrong: [
        'Option A: SQL injection goes through query parameters or request bodies, not CORS headers; these are entirely different attack vectors.',
        'Option C: CORS attacks exploit the browser\'s trust of cookies, not network-level flooding; DDoS is a volumetric availability attack unrelated to CORS.',
        'Option D: The Origin header is set by the browser, not by user-controlled scripts; it cannot be changed by JavaScript, so stealing cookies by altering it is not possible from a browser context.',
      ],
      example: 'Misconfigured: `Access-Control-Allow-Origin: *` + `Access-Control-Allow-Credentials: true`. A malicious page at evil.com runs `fetch("https://bank.com/api/transfer", { credentials: "include" })` — the browser sends the victim\'s bank cookies. Fix: only allow specific trusted origins, never `*` with credentials.',
    },
    {
      id: 'mdas-010',
      topic: 'Security',
      difficulty: 'easy',
      prompt: 'Why is input validation important on the server side?',
      options: [
        { id: 'a', text: 'Because the browser already validated the input' },
        { id: 'b', text: 'Client-side validation can be bypassed, so server-side validation is the authoritative check against malicious or malformed data' },
        { id: 'c', text: 'To improve database query performance' },
        { id: 'd', text: 'To reduce network payload size' },
      ],
      correctOptionId: 'b',
      definition: 'Server-side input validation is non-negotiable because client-side validation (JavaScript, HTML attributes) can be bypassed by any attacker using curl, Postman, or browser dev tools to send arbitrary data directly to the API.',
      explanation: 'Never trust the client. Client-side validation provides good UX (instant feedback without a round trip) but zero security. An attacker can skip your UI entirely and POST malformed or malicious data directly to your API endpoint. Server-side validation is the only security boundary. Defense in depth: validate on both sides, but only trust the server.',
      whyOthersAreWrong: [
        'Option A: The browser\'s client-side validation can be completely bypassed; any assumption that it already happened is a security vulnerability.',
        'Option C: Input validation is a correctness and security measure; it may add a small overhead and is not primarily a performance optimization.',
        'Option D: Validation has no meaningful effect on payload size; it is about verifying that the received data is valid and safe to process.',
      ],
      example: 'A registration form validates email client-side with a regex. An attacker bypasses the form: `curl -X POST https://api.example.com/register -d "email=\'; DROP TABLE users; --"`. Without server-side validation and prepared statements, this could be devastating. Client-side validation never ran.',
    },
    {
      id: 'mdas-011',
      topic: 'Query Optimization',
      difficulty: 'medium',
      prompt: 'What is rate limiting in the context of APIs?',
      options: [
        { id: 'a', text: 'Limiting the number of fields returned per response' },
        { id: 'b', text: 'Restricting the number of requests a client can make in a given time period to prevent abuse and ensure availability' },
        { id: 'c', text: 'Throttling database queries based on size' },
        { id: 'd', text: 'Capping the response payload to a maximum size' },
      ],
      correctOptionId: 'b',
      definition: 'Rate limiting restricts how many API requests a client (identified by IP, API key, or user ID) can make in a time window (e.g., 100 requests per minute), protecting the service from abuse, brute-force attacks, and unintentional overload.',
      explanation: 'Without rate limiting, a single client can flood an API with requests, degrading service for all users or enabling brute-force password attacks. Rate limiting is usually implemented at the API gateway or middleware level with a token bucket or sliding window algorithm. Clients that exceed the limit receive `429 Too Many Requests`.',
      whyOthersAreWrong: [
        'Option A: Limiting response fields is done through field selection or sparse fieldsets (GraphQL, JSON:API); this is about response shaping, not request frequency.',
        'Option C: Throttling database queries by size is a separate query optimization concern; rate limiting is about request frequency per client over time.',
        'Option D: Capping payload size is done via `Content-Length` limits in web server configuration; rate limiting is about request count per time period, not response size.',
      ],
      example: 'A public API allows 100 requests per minute per API key. When exceeded, it returns: `HTTP 429 Too Many Requests` with header `Retry-After: 42`. This prevents a single poorly-written client from DoS-ing the service and protects against credential brute-forcing.',
    },
    {
      id: 'mdas-012',
      topic: 'Auth/Authz',
      difficulty: 'medium',
      prompt: 'What is RBAC (Role-Based Access Control)?',
      options: [
        { id: 'a', text: "Restricting access based on the user's IP address" },
        { id: 'b', text: 'A model where permissions are assigned to roles and users are assigned roles, simplifying authorization management' },
        { id: 'c', text: "Encrypting user data based on their role" },
        { id: 'd', text: 'Requiring multi-factor authentication for admin roles' },
      ],
      correctOptionId: 'b',
      definition: 'Role-Based Access Control (RBAC) is an authorization model where permissions (capabilities) are assigned to roles (e.g., admin, editor, viewer) and users are assigned to roles, rather than permissions being assigned directly to individual users.',
      explanation: 'RBAC simplifies permission management at scale. Instead of managing permissions for each of 10,000 users individually, you define roles with permissions and assign users to roles. When a user\'s job changes, you change their role — not their individual permissions. Auditing is also simpler: checking what role a user has is one lookup.',
      whyOthersAreWrong: [
        'Option A: IP-based access restriction is a network-level control; RBAC is an application-level authorization model based on user roles, not network location.',
        'Option C: Encrypting data based on role is a separate data security concern; RBAC is about what actions or resources a user can access, not encryption.',
        'Option D: Multi-factor authentication (MFA) is an authentication mechanism; RBAC is an authorization model. They can coexist but are distinct concepts.',
      ],
      example: 'A CMS has roles: `admin` (can do everything), `editor` (can create/edit posts, cannot delete users), `viewer` (read-only). Assigning Alice to `editor` gives her all editor permissions instantly. Promoting her to `admin` changes one field, not 50 individual permissions.',
    },
    {
      id: 'mdas-013',
      topic: 'Security',
      difficulty: 'medium',
      prompt: 'What is secrets management?',
      options: [
        { id: 'a', text: 'Storing API keys in the source code as constants' },
        { id: 'b', text: 'Securely storing and accessing sensitive configuration (API keys, passwords, certificates) outside of code using tools like vaults or environment variables' },
        { id: 'c', text: 'Encrypting the entire database' },
        { id: 'd', text: 'Managing user passwords in a dedicated table' },
      ],
      correctOptionId: 'b',
      definition: 'Secrets management is the practice of securely storing, accessing, rotating, and auditing sensitive credentials (API keys, database passwords, TLS certificates) using dedicated tools rather than embedding them in source code or config files.',
      explanation: 'Secrets in source code get committed to version control and exposed to everyone with repository access (including third-party tools). Secrets management tools like HashiCorp Vault, AWS Secrets Manager, and Azure Key Vault provide encrypted storage, fine-grained access control, automatic rotation, and audit logs of who accessed which secret and when.',
      whyOthersAreWrong: [
        'Option A: Storing API keys as code constants is exactly the anti-pattern that secrets management prevents; secrets in source control are a critical security risk.',
        'Option C: Encrypting the entire database is full-disk encryption (encryption at rest); secrets management is about application-level credential handling, not database-level encryption.',
        'Option D: Managing user passwords is authentication infrastructure; secrets management is about the application\'s own credentials (service passwords, API keys), not end-user passwords.',
      ],
      example: 'Instead of `const DB_PASSWORD = "hunter2"` in code, use environment variables injected by the platform: `const DB_PASSWORD = process.env.DB_PASSWORD`. In production, populate from AWS Secrets Manager with automatic rotation every 30 days and audit logging.',
    },
    {
      id: 'mdas-014',
      topic: 'Schema Design',
      difficulty: 'medium',
      prompt: 'What does a Content Security Policy (CSP) header do?',
      options: [
        { id: 'a', text: 'Encrypts the page content before sending it' },
        { id: 'b', text: 'Instructs the browser which sources of scripts, styles, and other resources are trusted, helping prevent XSS attacks' },
        { id: 'c', text: 'Blocks all cross-origin requests' },
        { id: 'd', text: 'Verifies the identity of the server' },
      ],
      correctOptionId: 'b',
      definition: 'Content Security Policy (CSP) is an HTTP response header that tells browsers which resource origins (scripts, styles, images, etc.) are allowed, preventing browsers from loading and executing resources from untrusted sources.',
      explanation: 'XSS (Cross-Site Scripting) attacks inject malicious scripts into pages. CSP mitigates this by whitelisting trusted script sources. Even if an attacker injects a `<script src="evil.com/hack.js">` tag, the browser will refuse to load it if `evil.com` is not in the CSP. A strict CSP (`script-src "self"` only) is one of the strongest XSS defenses.',
      whyOthersAreWrong: [
        'Option A: Content encryption is TLS/HTTPS; CSP is a browser policy header that controls resource loading, not encryption.',
        'Option C: CSP controls resource loading policy, not all cross-origin requests. CORS controls cross-origin API requests; CSP controls which script/style/media sources are permitted.',
        'Option D: Server identity verification is done via TLS certificates (HTTPS); CSP is about controlling which third-party resources the browser will load.',
      ],
      example: 'HTTP header: `Content-Security-Policy: default-src "self"; script-src "self" https://cdn.trusted.com; object-src "none"`. This allows scripts only from the same origin and `cdn.trusted.com`, blocks plugins entirely, and prevents loading resources from anywhere else.',
    },
    {
      id: 'mdas-015',
      topic: 'Indexing',
      difficulty: 'hard',
      prompt: 'Why can having too many indexes on a table be harmful?',
      options: [
        { id: 'a', text: 'Indexes slow down read queries' },
        { id: 'b', text: 'Each index must be updated on every INSERT, UPDATE, or DELETE, increasing write overhead and storage costs' },
        { id: 'c', text: 'Indexes prevent foreign key constraints from working' },
        { id: 'd', text: 'Too many indexes cause query plan confusion and always result in full table scans' },
      ],
      correctOptionId: 'b',
      definition: 'Each index on a table is a separate data structure that must be maintained — every write operation (INSERT, UPDATE, DELETE) must update all indexes on the affected columns, adding latency and storage overhead.',
      explanation: 'A table with 10 indexes on a write-heavy table (e.g., an event log receiving millions of inserts per second) will be significantly slower for writes than the same table with 2 targeted indexes. Additionally, each index consumes disk space. The right number of indexes depends on the read/write ratio and specific query patterns.',
      whyOthersAreWrong: [
        'Option A: Indexes speed up read queries — this is the opposite of the truth. The harm is on writes, not reads.',
        'Option C: Indexes and foreign key constraints are independent; indexes on foreign key columns are actually recommended for JOIN performance and have no negative interaction with the constraints themselves.',
        'Option D: Modern query optimizers are sophisticated; they evaluate indexes and choose the best plan. Having many indexes does not always lead to full table scans — though index selection complexity does increase, it does not always result in the worst outcome.',
      ],
      example: 'A high-throughput events table with indexes on 8 columns suffers on ingestion. Profiling shows each INSERT takes 50ms instead of 5ms because 8 B-tree index structures are updated per write. Dropping unused indexes reduces INSERT latency by 80% with no change to frequently-run read queries.',
    },
  ],
};
